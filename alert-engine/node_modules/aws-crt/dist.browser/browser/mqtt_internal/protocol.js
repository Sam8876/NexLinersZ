"use strict";
/*
 * Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0.
 */
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.foldTimeMax = exports.foldTimeMin = exports.ProtocolState = exports.HaltEventType = exports.OperationQueueType = exports.UserEventType = exports.NetworkEventType = exports.ProtocolStateType = exports.OfflineQueuePolicy = exports.ResumeSessionPolicyType = exports.PublishResultType = void 0;
var mqtt5_packet = __importStar(require("../../common/mqtt5_packet"));
var mqtt5_packet_1 = require("../../common/mqtt5_packet");
var model = __importStar(require("./model"));
var encoder = __importStar(require("./encoder"));
var decoder = __importStar(require("./decoder"));
var heap = __importStar(require("../heap"));
var validate = __importStar(require("./validate"));
var error_1 = require("../error");
var io_1 = require("../../common/io");
var mqtt5 = __importStar(require("../mqtt5"));
var mqtt_shared = __importStar(require("../../common/mqtt_shared"));
var event_1 = require("../../common/event");
var uuid_1 = require("uuid");
/**
 * Algebraic union indicator type for what is in the result of a publish operation
 */
var PublishResultType;
(function (PublishResultType) {
    PublishResultType[PublishResultType["Qos0"] = 0] = "Qos0";
    PublishResultType[PublishResultType["Qos1"] = 1] = "Qos1";
})(PublishResultType = exports.PublishResultType || (exports.PublishResultType = {}));
/**
 * Controls how the client will attempt to use MQTT sessions.
 */
var ResumeSessionPolicyType;
(function (ResumeSessionPolicyType) {
    /** User clean start true until a successful connection is established.  Afterwards, always attempt to rejoin a session */
    ResumeSessionPolicyType[ResumeSessionPolicyType["PostSuccess"] = 0] = "PostSuccess";
    /** Never rejoin a session.  Clean start is always true. */
    ResumeSessionPolicyType[ResumeSessionPolicyType["Never"] = 1] = "Never";
    /** Always try to rejoin a session.  Clean start is always false.  This setting is technically not spec-compliant */
    ResumeSessionPolicyType[ResumeSessionPolicyType["Always"] = 2] = "Always";
    /** Option to use when no option is specified. */
    ResumeSessionPolicyType[ResumeSessionPolicyType["Default"] = 0] = "Default";
})(ResumeSessionPolicyType = exports.ResumeSessionPolicyType || (exports.ResumeSessionPolicyType = {}));
/**
 * Controls how disconnects affect the queued and in-progress operations tracked by the client.  Also controls
 * how operations are handled while the client is not connected.  In particular, if the client is not connected,
 * then any operation that would be failed on disconnect (according to these rules) will be rejected.
 *
 * A deliberate mirror of the native ClientOperationQueueBehavior enum
 */
var OfflineQueuePolicy;
(function (OfflineQueuePolicy) {
    /** Operations are never failed due to connection state */
    OfflineQueuePolicy[OfflineQueuePolicy["PreserveAll"] = 0] = "PreserveAll";
    /** Qos0 Publishes are failed when there is no connection, all other operations are left alone. */
    OfflineQueuePolicy[OfflineQueuePolicy["PreserveAcknowledged"] = 1] = "PreserveAcknowledged";
    /** Only QoS1 and QoS2 publishes are retained when there is no connection */
    OfflineQueuePolicy[OfflineQueuePolicy["PreserveQos1PlusPublishes"] = 2] = "PreserveQos1PlusPublishes";
    /** Nothing is retained when there is no connection */
    OfflineQueuePolicy[OfflineQueuePolicy["PreserveNothing"] = 3] = "PreserveNothing";
    /** Keep everything by default */
    OfflineQueuePolicy[OfflineQueuePolicy["Default"] = 0] = "Default";
})(OfflineQueuePolicy = exports.OfflineQueuePolicy || (exports.OfflineQueuePolicy = {}));
/**
 * States that the protocol object can be in
 */
var ProtocolStateType;
(function (ProtocolStateType) {
    /** Not connected to anything */
    ProtocolStateType[ProtocolStateType["Disconnected"] = 0] = "Disconnected";
    /** Transport connected, but connect-connack handshake has not completed */
    ProtocolStateType[ProtocolStateType["PendingConnack"] = 1] = "PendingConnack";
    /** An MQTT connection has been successfully established */
    ProtocolStateType[ProtocolStateType["Connected"] = 2] = "Connected";
})(ProtocolStateType = exports.ProtocolStateType || (exports.ProtocolStateType = {}));
/**
 * Different network-related events that the protocol implementation is interested in
 */
var NetworkEventType;
(function (NetworkEventType) {
    /** A transport connection has been successfully established */
    NetworkEventType[NetworkEventType["ConnectionOpened"] = 0] = "ConnectionOpened";
    /** The current transport connection has been closed */
    NetworkEventType[NetworkEventType["ConnectionClosed"] = 1] = "ConnectionClosed";
    /** Data has been received on the current connection */
    NetworkEventType[NetworkEventType["IncomingData"] = 2] = "IncomingData";
    /** Outbound data from the protocol state has been flushed to the socket */
    NetworkEventType[NetworkEventType["WriteCompletion"] = 3] = "WriteCompletion";
})(NetworkEventType = exports.NetworkEventType || (exports.NetworkEventType = {}));
/**
 * Discriminated union tag type for the different kind of user events that the protocol state handles.
 */
var UserEventType;
(function (UserEventType) {
    /** Send a publish packet */
    UserEventType[UserEventType["Publish"] = 0] = "Publish";
    /** Send a subscribe packet */
    UserEventType[UserEventType["Subscribe"] = 1] = "Subscribe";
    /** Send an unsubscribe packet */
    UserEventType[UserEventType["Unsubscribe"] = 2] = "Unsubscribe";
    /** Send a disconnect packet and halt the protocol state */
    UserEventType[UserEventType["Disconnect"] = 3] = "Disconnect";
})(UserEventType = exports.UserEventType || (exports.UserEventType = {}));
function isUserOperationType(type) {
    switch (type) {
        case mqtt5_packet.PacketType.Publish:
        case mqtt5_packet.PacketType.Subscribe:
        case mqtt5_packet.PacketType.Unsubscribe:
        case mqtt5_packet.PacketType.Disconnect:
            return true;
        default:
            return false;
    }
}
/**
 * Type of operation queue.
 *
 * The protocol implementation contains three seperate queues, that are ordered by priority.
 */
var OperationQueueType;
(function (OperationQueueType) {
    /**
     * The high priority queue contains critical packets which must go out immediately:
     *     Connect, Puback, Disconnect, Pingreq
     */
    OperationQueueType[OperationQueueType["HighPriority"] = 0] = "HighPriority";
    /**
     * The resubmit queue contains QoS1+ publishes which must be resent on reconnection as required by spec
     */
    OperationQueueType[OperationQueueType["Resubmit"] = 1] = "Resubmit";
    /**
     * All other operations fall into the user queue, which is the lowest priority queue
     */
    OperationQueueType[OperationQueueType["User"] = 2] = "User";
})(OperationQueueType = exports.OperationQueueType || (exports.OperationQueueType = {}));
var QueueEndType;
(function (QueueEndType) {
    QueueEndType[QueueEndType["Front"] = 0] = "Front";
    QueueEndType[QueueEndType["Back"] = 1] = "Back";
})(QueueEndType || (QueueEndType = {}));
var ServiceQueueType;
(function (ServiceQueueType) {
    ServiceQueueType[ServiceQueueType["HighPriorityOnly"] = 0] = "HighPriorityOnly";
    ServiceQueueType[ServiceQueueType["All"] = 1] = "All";
})(ServiceQueueType || (ServiceQueueType = {}));
function compareTimeoutRecords(lhs, rhs) {
    if (lhs.timeoutElapsedMillis < rhs.timeoutElapsedMillis) {
        return true;
    }
    else if (lhs.timeoutElapsedMillis > rhs.timeoutElapsedMillis) {
        return false;
    }
    else {
        return lhs.operationId < rhs.operationId;
    }
}
/**
 * Classifies why the client has been halted.
 */
var HaltEventType;
(function (HaltEventType) {
    /**
     * The client has been halted due to an event not considered anomalous: user-initiated or server-initiated disconnect, rejected connection attempt, etc...
     */
    HaltEventType[HaltEventType["Normal"] = 0] = "Normal";
    /**
     * The client has been halted due to unexpected, spec-breaking behavior from the remote broker
     */
    HaltEventType[HaltEventType["ProtocolError"] = 1] = "ProtocolError";
    /**
     * The client has been halted due to an unexpected internal state or exception
     */
    HaltEventType[HaltEventType["Unknown"] = 2] = "Unknown";
    /**
     * The client has been halted due to a network timeout (connack or ping)
     */
    HaltEventType[HaltEventType["Timeout"] = 3] = "Timeout";
})(HaltEventType = exports.HaltEventType || (exports.HaltEventType = {}));
var MAXIMUM_NUMBER_OF_PACKET_IDS = 65535;
var MAXIMUM_PACKET_ID = 65535;
var PROTOCOL_STATE_LOG_SUBJECT = "MQTT ProtocolState";
/**
 * Encapsulates all MQTT  protocol-related behavior over the course of repeated connections to a remote broker.
 */
var ProtocolState = /** @class */ (function (_super) {
    __extends(ProtocolState, _super);
    function ProtocolState(config) {
        var _this = _super.call(this) || this;
        _this.state = ProtocolStateType.Disconnected;
        _this.elapsedMillis = 0;
        _this.pendingConnackTimeoutElapsedMillis = undefined;
        _this.nextOutboundPingElapsedMillis = undefined;
        _this.pendingPingrespTimeoutElapsedMillis = undefined;
        _this.nextOperationId = 1;
        _this.operations = new Map();
        _this.operationTimeouts = new heap.MinHeap(compareTimeoutRecords);
        _this.userOperationQueue = new Array();
        _this.resubmitOperationQueue = new Array();
        _this.highPriorityOperationQueue = new Array();
        _this.currentOperation = undefined;
        _this.nextPacketId = 1;
        _this.boundPacketIds = new Map();
        _this.pendingWriteCompletion = false;
        _this.pendingWriteCompletionOperations = new Array();
        _this.pendingFlushOperations = new Array();
        _this.pendingAcks = new Map();
        _this.unackedPublishCount = 0;
        _this.nextPublishAcknowledgementControlId = 1;
        _this.pendingPublishAcknowledgements = new Map();
        _this.pendingPublishAcknowledgementsByPacketId = new Map();
        _this.lastNegotiatedSettings = createDefaultNegotiatedSettings();
        _this.lastOutboundConnect = createDefaultConnect();
        _this.hasSuccessfullyConnected = false;
        if (!config.connectOptions.clientId) {
            config.connectOptions.clientId = "autogen-".concat((0, uuid_1.v4)());
        }
        _this.config = config;
        _this.encoder = new encoder.Encoder(encoder.buildClientEncodingFunctionSet(config.protocolVersion));
        _this.decoder = new decoder.Decoder(decoder.buildClientDecodingFunctionSet(config.protocolVersion));
        _this.elapsedMillis = config.baseElapsedMillis;
        return _this;
    }
    /**
     * Handle a network-related event:
     *   connection open/close, incoming data, and socket write completion
     *
     * @param context information about the event
     */
    ProtocolState.prototype.handleNetworkEvent = function (context) {
        this.cork();
        try {
            this.updateElapsedMillis(context.elapsedMillis);
            switch (context.type) {
                case NetworkEventType.ConnectionOpened:
                    this.handleConnectionOpened(context.context);
                    break;
                case NetworkEventType.ConnectionClosed:
                    this.handleConnectionClosed();
                    break;
                case NetworkEventType.IncomingData:
                    this.handleIncomingData(context);
                    break;
                case NetworkEventType.WriteCompletion:
                    this.handleWriteCompletion();
                    break;
            }
        }
        catch (e) {
            this.halt(HaltEventType.Unknown, new error_1.CrtError("handleNetworkEvent() failure: ".concat(e)));
        }
        finally {
            this.uncork();
        }
    };
    /**
     * Handle a user-submitted operation:
     *   Publish, Subscribe, Unsubscribe, or Disconnect
     *
     * @param context information about the operation
     */
    ProtocolState.prototype.handleUserEvent = function (context) {
        this.cork();
        try {
            this.updateElapsedMillis(context.elapsedMillis);
            switch (context.type) {
                case UserEventType.Publish:
                    this.submitPublish(context.context);
                    break;
                case UserEventType.Subscribe:
                    this.submitSubscribe(context.context);
                    break;
                case UserEventType.Unsubscribe:
                    this.submitUnsubscribe(context.context);
                    break;
                case UserEventType.Disconnect:
                    this.submitDisconnect(context.context);
                    break;
            }
        }
        finally {
            this.uncork();
        }
    };
    /**
     * Function that drives time-based protocol state processing
     *
     * @param context service context
     */
    ProtocolState.prototype.service = function (context) {
        this.cork();
        try {
            this.updateElapsedMillis(context.elapsedMillis);
            if (!this.haltState) {
                switch (this.state) {
                    case ProtocolStateType.PendingConnack:
                        return this.servicePendingConnack(context);
                    case ProtocolStateType.Connected:
                        return this.serviceConnected(context);
                    default:
                        break;
                }
            }
        }
        catch (e) {
            this.halt(HaltEventType.Unknown, new error_1.CrtError("service() failure: ".concat(e)));
        }
        finally {
            this.uncork();
        }
        return {};
    };
    /**
     * Calculates the next time that the service function should be invoked, based on current state
     *
     * @param elapsedMillis current elapsed time
     */
    ProtocolState.prototype.getNextServiceTimepoint = function (elapsedMillis) {
        this.updateElapsedMillis(elapsedMillis);
        switch (this.state) {
            case ProtocolStateType.PendingConnack:
                return this.getNextServiceTimepointPendingConnack();
            case ProtocolStateType.Connected:
                return this.getNextServiceTimepointConnected();
            default:
                return undefined;
        }
    };
    /* Test accessors */
    ProtocolState.prototype.getState = function () { return this.state; };
    ProtocolState.prototype.getHaltState = function () { return this.haltState; };
    ProtocolState.prototype.getPendingConnackTimeoutElapsedMillis = function () { return this.pendingConnackTimeoutElapsedMillis; };
    ProtocolState.prototype.getNextOutboundPingElapsedMillis = function () { return this.nextOutboundPingElapsedMillis; };
    ProtocolState.prototype.getPendingPingrespTimeoutElapsedMillis = function () { return this.pendingPingrespTimeoutElapsedMillis; };
    ProtocolState.prototype.getOperations = function () { return this.operations; };
    ProtocolState.prototype.getOperationTimeouts = function () { return this.operationTimeouts; };
    ProtocolState.prototype.getCurrentOperation = function () {
        var id = this.currentOperation;
        if (id != undefined) {
            return this.operations.get(id);
        }
        return undefined;
    };
    ProtocolState.prototype.getConfig = function () { return this.config; };
    ProtocolState.prototype.getOperationQueue = function (type) {
        switch (type) {
            case OperationQueueType.User:
                return this.userOperationQueue;
            case OperationQueueType.Resubmit:
                return this.resubmitOperationQueue;
            case OperationQueueType.HighPriority:
                return this.highPriorityOperationQueue;
            default:
                throw new error_1.CrtError("Unknown operation queue type");
        }
    };
    ProtocolState.prototype.getBoundPacketIds = function () { return this.boundPacketIds; };
    ProtocolState.prototype.getPendingWriteCompletion = function () { return this.pendingWriteCompletion; };
    ProtocolState.prototype.getPendingWriteCompletionOperations = function () { return this.pendingWriteCompletionOperations; };
    ProtocolState.prototype.getPendingFlushOperations = function () { return this.pendingFlushOperations; };
    ProtocolState.prototype.getPendingAcks = function () { return this.pendingAcks; };
    ProtocolState.prototype.getUnackedPublishCount = function () { return this.unackedPublishCount; };
    ProtocolState.prototype.getPendingPublishAcknowledgements = function () { return this.pendingPublishAcknowledgements; };
    ProtocolState.prototype.getPendingPublishAcknowledgementsByPacketId = function () { return this.pendingPublishAcknowledgementsByPacketId; };
    ProtocolState.prototype.on = function (event, listener) {
        _super.prototype.on.call(this, event, listener);
        return this;
    };
    /* Internal Implementation */
    /**
     * Reset ping-related state.  Invoked after a Pingreq is sent.
     */
    ProtocolState.prototype.resetNextPing = function () {
        (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "Resetting next ping state");
        this.pendingPingrespTimeoutElapsedMillis = undefined;
        this.pushOutNextPing(this.elapsedMillis);
    };
    /**
     * Update the state to not send a pingreq until later due to the receipt of a successful ack,
     * which demonstrates a healthy connection at the time the original operation was sent.
     */
    ProtocolState.prototype.pushOutNextPing = function (baseTime) {
        var _this = this;
        if (!this.config.connectOptions.keepAliveIntervalSeconds) {
            return;
        }
        if (baseTime != undefined) {
            var pingTime = baseTime + this.config.connectOptions.keepAliveIntervalSeconds * 1000;
            this.nextOutboundPingElapsedMillis = foldTimeMax(this.nextOutboundPingElapsedMillis, pingTime);
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { var _a; return "Adjusting next ping time to ".concat(((_a = _this.nextOutboundPingElapsedMillis) !== null && _a !== void 0 ? _a : 0) - _this.elapsedMillis, " millis in the future"); });
        }
    };
    /**
     * Releases a packet id, allowing it to be reused in outbound packets
     */
    ProtocolState.prototype.unbindPacketId = function (id) {
        if (id == undefined) {
            return;
        }
        (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Releasing packet id ".concat(id); });
        this.boundPacketIds.delete(id);
        this.pendingAcks.delete(id);
    };
    ProtocolState.prototype.onOperationFinished = function (operation) {
        var _a;
        this.unbindPacketId(operation.packetId);
        if (operation.type == mqtt5_packet.PacketType.Publish && ((_a = operation.packetId) !== null && _a !== void 0 ? _a : 0) != 0) {
            this.unackedPublishCount--;
        }
    };
    ProtocolState.prototype.failOperation = function (id, error) {
        (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Failing operation ".concat(id, " with error \"").concat(error.toString(), "\""); });
        var operation = this.operations.get(id);
        if (operation) {
            this.onOperationFinished(operation);
            if (operation.options) {
                operation.options.resultHandler.onCompletionFailure(error);
            }
            this.operations.delete(id);
        }
    };
    ProtocolState.prototype.completeOperation = function (id, result) {
        (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Completing operation ".concat(id, " successfully"); });
        var operation = this.operations.get(id);
        if (operation) {
            this.onOperationFinished(operation);
            if (operation.options) {
                switch (operation.type) {
                    case mqtt5_packet.PacketType.Publish:
                        var publishHandler = operation.options.resultHandler;
                        if (result) {
                            publishHandler.onCompletionSuccess(result);
                        }
                        else {
                            publishHandler.onCompletionSuccess({
                                type: PublishResultType.Qos0
                            });
                        }
                        break;
                    case mqtt5_packet.PacketType.Subscribe:
                        var subscribeHandler = operation.options.resultHandler;
                        subscribeHandler.onCompletionSuccess(result);
                        break;
                    case mqtt5_packet.PacketType.Unsubscribe:
                        var unsubscribeHandler = operation.options.resultHandler;
                        unsubscribeHandler.onCompletionSuccess(result);
                        break;
                    default:
                        var genericOptions = operation.options;
                        genericOptions.resultHandler.onCompletionSuccess();
                        break;
                }
            }
            this.operations.delete(id);
        }
    };
    ProtocolState.prototype.changeState = function (newState) {
        var _this = this;
        if (newState != this.state) {
            (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Changing state from ".concat(ProtocolStateType[_this.state], " to ").concat(ProtocolStateType[newState]); });
            this.state = newState;
        }
    };
    /**
     * Gets the next service timepoint when in the pending connack state.  This factors in the high-priority queue
     * (connect) and the pending connack timeout.
     */
    ProtocolState.prototype.getNextServiceTimepointPendingConnack = function () {
        return foldTimeMin(this.getQueueServiceTimepoint(ServiceQueueType.HighPriorityOnly), this.pendingConnackTimeoutElapsedMillis);
    };
    /**
     * Gets the next service timepoint when in the connected state.  This factors in all operation queues,
     * ping functionality, and operation timeouts.
     */
    ProtocolState.prototype.getNextServiceTimepointConnected = function () {
        var _a;
        var serviceTime = this.getQueueServiceTimepoint(ServiceQueueType.All);
        serviceTime = foldTimeMin(serviceTime, this.nextOutboundPingElapsedMillis);
        serviceTime = foldTimeMin(serviceTime, this.pendingPingrespTimeoutElapsedMillis);
        serviceTime = foldTimeMin(serviceTime, (_a = this.operationTimeouts.peek()) === null || _a === void 0 ? void 0 : _a.timeoutElapsedMillis);
        return serviceTime;
    };
    /**
     * Gets the next service timepoint relative to operation queues.
     */
    ProtocolState.prototype.getQueueServiceTimepoint = function (serviceQueueType) {
        // we don't service queued operations when there is an outstanding socket write
        if (this.pendingWriteCompletion) {
            return undefined;
        }
        // if we're in the middle of an operation, service now
        if (this.currentOperation != undefined) {
            return this.elapsedMillis;
        }
        switch (serviceQueueType) {
            case ServiceQueueType.HighPriorityOnly:
                return (this.highPriorityOperationQueue.length > 0) ? this.elapsedMillis : undefined;
            case ServiceQueueType.All:
                if (this.highPriorityOperationQueue.length > 0 || this.resubmitOperationQueue.length > 0) {
                    return this.elapsedMillis;
                }
                // user operations require more logic since there are certain conditions that we cannot
                // dequeue them (receive maximum limit, no packet ids, etc...)
                var id = this.userOperationQueue.shift();
                if (id != undefined) {
                    // there is no peek API, we have to pop and push back
                    this.userOperationQueue.unshift(id);
                    if (this.canDequeueUserOperation(id)) {
                        return this.elapsedMillis;
                    }
                }
                return undefined;
            default:
                return undefined;
        }
    };
    ProtocolState.prototype.canAllocatePacketId = function () {
        return this.pendingAcks.size < MAXIMUM_NUMBER_OF_PACKET_IDS;
    };
    ProtocolState.prototype.wouldOperationBreachReceiveMaximum = function (id) {
        var operation = this.operations.get(id);
        if (!operation) {
            return false;
        }
        if (operation.type != mqtt5_packet.PacketType.Publish) {
            return false;
        }
        var publishPacket = operation.packet;
        if (publishPacket.qos != mqtt5_packet.QoS.AtMostOnce) {
            if (this.unackedPublishCount >= this.lastNegotiatedSettings.receiveMaximumFromServer) {
                return true;
            }
        }
        return false;
    };
    ProtocolState.prototype.canDequeueUserOperation = function (id) {
        if (this.operationNeedsPacketBinding(id) && !this.canAllocatePacketId()) {
            return false;
        }
        if (this.wouldOperationBreachReceiveMaximum(id)) {
            return false;
        }
        return true;
    };
    ProtocolState.prototype.dequeueNextOperation = function (serviceQueueType) {
        if (this.pendingWriteCompletion) {
            return undefined;
        }
        var id = undefined;
        switch (serviceQueueType) {
            case ServiceQueueType.HighPriorityOnly:
                id = this.highPriorityOperationQueue.shift();
                break;
            case ServiceQueueType.All:
                id = this.highPriorityOperationQueue.shift();
                if (id == undefined) {
                    id = this.resubmitOperationQueue.shift();
                }
                if (id == undefined) {
                    id = this.userOperationQueue.shift();
                    if (id != undefined) {
                        if (!this.canDequeueUserOperation(id)) {
                            this.userOperationQueue.unshift(id);
                            id = undefined;
                        }
                    }
                }
                break;
            default:
                return undefined;
        }
        if (id == undefined) {
            return undefined;
        }
        return this.operations.get(id);
    };
    /**
     * Invoked on an operation after it is fully encoded to an output buffer.
     */
    ProtocolState.prototype.onOperationProcessed = function (operation) {
        (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Operation ".concat(operation.id, " moved to post-processed state"); });
        operation.numAttempts++;
        this.pendingFlushOperations.push(operation.id);
        var timeoutMillis = undefined;
        switch (operation.type) {
            case mqtt5_packet.PacketType.Publish:
                if (operation.packetId != undefined) {
                    // track the expected ack for qos 1 completion
                    this.unackedPublishCount++;
                    this.pendingAcks.set(operation.packetId, operation.id);
                    var publishOptions = operation.options;
                    timeoutMillis = publishOptions.options.timeoutInMillis;
                }
                else {
                    // qos 0, complete the operation on write completion
                    this.pendingWriteCompletionOperations.push(operation.id);
                }
                break;
            case mqtt5_packet.PacketType.Subscribe:
                if (operation.packetId != undefined) {
                    // setup operation completion for corresponding suback
                    this.pendingAcks.set(operation.packetId, operation.id);
                    var subscribeOptions = operation.options;
                    timeoutMillis = subscribeOptions.options.timeoutInMillis;
                }
                else {
                    this.halt(HaltEventType.Unknown, new error_1.CrtError("Packet id not set for outbound subscribe"));
                }
                break;
            case mqtt5_packet.PacketType.Unsubscribe:
                if (operation.packetId != undefined) {
                    // setup operation completion for corresponding unsuback
                    this.pendingAcks.set(operation.packetId, operation.id);
                    var unsubscribeOptions = operation.options;
                    timeoutMillis = unsubscribeOptions.options.timeoutInMillis;
                }
                else {
                    this.halt(HaltEventType.Unknown, new error_1.CrtError("Packet id not set for outbound unsubscribe"));
                }
                break;
            case mqtt5_packet.PacketType.Disconnect:
                // complete the operation on write completion
                this.pendingWriteCompletionOperations.push(operation.id);
                // this is the last thing to be processed before halting
                this.halt(HaltEventType.Normal, new error_1.CrtError("User-initiated disconnect"));
                break;
            default:
                // pingreq, connect, puback
                this.pendingWriteCompletionOperations.push(operation.id);
                break;
        }
        if (timeoutMillis) {
            this.operationTimeouts.push({
                operationId: operation.id,
                timeoutElapsedMillis: this.elapsedMillis + timeoutMillis
            });
        }
        return this.haltState != undefined;
    };
    ProtocolState.prototype.operationNeedsPacketBinding = function (id) {
        var operation = this.operations.get(id);
        if (!operation) {
            return false;
        }
        if (operation.packetId != undefined) {
            return false;
        }
        switch (operation.type) {
            case mqtt5_packet.PacketType.Subscribe:
            case mqtt5_packet.PacketType.Unsubscribe:
                return true;
            case mqtt5_packet.PacketType.Publish:
                var publish = operation.packet;
                return publish.qos != 0;
            default:
                return false;
        }
    };
    ProtocolState.prototype.advanceNextPacketId = function () {
        var packetId = this.nextPacketId++;
        if (this.nextPacketId > MAXIMUM_PACKET_ID) {
            this.nextPacketId = 1;
        }
        return packetId;
    };
    ProtocolState.prototype.allocatePacketId = function (operationId) {
        var packetId = this.advanceNextPacketId();
        var startingId = packetId;
        while (this.boundPacketIds.has(packetId)) {
            packetId = this.advanceNextPacketId();
            if (packetId == startingId) {
                // should never happen because we check that there's room before calling this
                throw new error_1.CrtError("Packet id space exhausted");
            }
        }
        (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Packet id ".concat(packetId, " bound to operation ").concat(operationId); });
        this.boundPacketIds.set(packetId, operationId);
        return packetId;
    };
    ProtocolState.prototype.bindPacketId = function (operation) {
        if (!this.operationNeedsPacketBinding(operation.id)) {
            return;
        }
        operation.packetId = this.allocatePacketId(operation.id);
        switch (operation.type) {
            case mqtt5_packet.PacketType.Publish:
                operation.packet.packetId = operation.packetId;
                break;
            case mqtt5_packet.PacketType.Subscribe:
                operation.packet.packetId = operation.packetId;
                break;
            case mqtt5_packet.PacketType.Unsubscribe:
                operation.packet.packetId = operation.packetId;
                break;
            default:
                this.halt(HaltEventType.Unknown, new error_1.CrtError("Invalid operation for packet id binding"));
                break;
        }
    };
    ProtocolState.prototype.serviceOutboundOperations = function (serviceQueueType, socketBuffer) {
        (0, io_1.logDebug)(PROTOCOL_STATE_LOG_SUBJECT, "Begin servicing outbound operations");
        var done = false;
        var remainingView = new DataView(socketBuffer);
        var _loop_1 = function () {
            var currentOperation = undefined;
            if (this_1.currentOperation != undefined) {
                currentOperation = this_1.operations.get(this_1.currentOperation);
            }
            while (currentOperation == undefined) {
                currentOperation = this_1.dequeueNextOperation(serviceQueueType);
                if (currentOperation == undefined) {
                    break;
                }
                this_1.bindPacketId(currentOperation);
                var validationError = undefined;
                try {
                    validate.validateBinaryOutboundPacket(currentOperation.packet, this_1.config.protocolVersion, this_1.lastNegotiatedSettings);
                }
                catch (e) {
                    validationError = e;
                }
                if (validationError == undefined) {
                    // @ts-ignore
                    (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Initializing encoder state for operation ".concat(currentOperation.id); });
                    this_1.currentOperation = currentOperation.id;
                    this_1.encoder.initForPacket(currentOperation.packet);
                }
                else {
                    this_1.failOperation(currentOperation.id, new error_1.CrtError("Binary outbound packet validation failed: ".concat(validationError)));
                    currentOperation = undefined;
                }
            }
            if (currentOperation == undefined) {
                // nothing to do
                done = true;
            }
            else {
                (0, io_1.logDebug)(PROTOCOL_STATE_LOG_SUBJECT, "Encoding current operation");
                var encodeResult = this_1.encoder.service(remainingView);
                if (encodeResult.type == encoder.ServiceResultType.InProgress) {
                    // ran out of room in the output buffer
                    done = true;
                }
                else {
                    this_1.currentOperation = undefined;
                    done = this_1.onOperationProcessed(currentOperation);
                }
                remainingView = encodeResult.nextView;
            }
        };
        var this_1 = this;
        while (!done) {
            _loop_1();
        }
        var result = {};
        if (remainingView.byteLength < socketBuffer.byteLength) {
            // there's data that should be written to the socket.  That's the caller's responsibility.
            var byteLength_1 = socketBuffer.byteLength - remainingView.byteLength;
            result.toSocket = new DataView(socketBuffer, 0, byteLength_1);
            this.pendingWriteCompletion = true;
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Outbound operation processing generated ".concat(byteLength_1, " bytes to be written"); });
        }
        (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "End servicing outbound operations"; });
        return result;
    };
    ProtocolState.prototype.servicePendingConnack = function (context) {
        // check for and handle connack timeout
        if (this.pendingConnackTimeoutElapsedMillis != undefined) {
            if (this.elapsedMillis >= this.pendingConnackTimeoutElapsedMillis) {
                (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "Connack timeout breached");
                this.halt(HaltEventType.Timeout, new error_1.CrtError("Connack timeout"));
                return {};
            }
        }
        // service high-priority operations only
        return this.serviceOutboundOperations(ServiceQueueType.HighPriorityOnly, context.socketBuffer);
    };
    ProtocolState.prototype.servicePing = function () {
        var _a;
        // should we use ping at all?
        if (!this.config.connectOptions.keepAliveIntervalSeconds) {
            return;
        }
        // check for and handle sending a pingreq
        if (this.nextOutboundPingElapsedMillis) {
            if (this.elapsedMillis >= this.nextOutboundPingElapsedMillis) {
                (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "Next ping timeout exceeded.  Queuing Pingreq packet.");
                var pingreq = {
                    type: mqtt5_packet.PacketType.Pingreq
                };
                this.submitOperationHighPriority(pingreq, QueueEndType.Front);
                this.pushOutNextPing(this.elapsedMillis);
                var timeoutMillis_1 = (_a = foldTimeMin(this.config.connectOptions.keepAliveIntervalSeconds * 1000 / 2, this.config.pingTimeoutMillis)) !== null && _a !== void 0 ? _a : 0;
                this.pendingPingrespTimeoutElapsedMillis = this.elapsedMillis + timeoutMillis_1;
                (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Pingresp timeout in ".concat(timeoutMillis_1, " millis"); });
            }
        }
        // check for and handle a pingresp timeout
        if (this.pendingPingrespTimeoutElapsedMillis) {
            if (this.elapsedMillis >= this.pendingPingrespTimeoutElapsedMillis) {
                (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "Pingresp timeout exceeded.");
                this.halt(HaltEventType.Timeout, new error_1.CrtError("Pingresp timeout"));
            }
        }
    };
    ProtocolState.prototype.serviceOperationTimeouts = function () {
        var _loop_2 = function () {
            var top_1 = this_2.operationTimeouts.peek();
            if (top_1 == undefined) {
                return "break";
            }
            if (top_1.timeoutElapsedMillis > this_2.elapsedMillis) {
                return "break";
            }
            // @ts-ignore
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Operation ".concat(top_1.operationId, " timeout breached"); });
            this_2.operationTimeouts.pop();
            this_2.failOperation(top_1.operationId, new error_1.CrtError("Operation timed out"));
        };
        var this_2 = this;
        while (!this.operationTimeouts.empty()) {
            var state_1 = _loop_2();
            if (state_1 === "break")
                break;
        }
    };
    ProtocolState.prototype.serviceConnected = function (context) {
        this.servicePing();
        this.serviceOperationTimeouts();
        return this.serviceOutboundOperations(ServiceQueueType.All, context.socketBuffer);
    };
    ProtocolState.prototype.updateElapsedMillis = function (elapsedMillis) {
        var _this = this;
        if (elapsedMillis != this.elapsedMillis) {
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Elapsed millis updated from ".concat(_this.elapsedMillis, " to ").concat(elapsedMillis); });
            this.elapsedMillis = elapsedMillis;
        }
    };
    /**
     * Entry point for all operation submissions, both user and internal
     *
     * @param operation operation to add to an operation queue
     * @param userPacket original packet if this is a user operation.  Internal operations skip validation.
     * @param queueType what operation queue to submit to
     * @param submitLocation whether to submit to the front of back of the queue
     * @private
     */
    ProtocolState.prototype.submitOperation = function (operation, packet, queueType, submitLocation) {
        this.operations.set(operation.id, operation);
        try {
            // perform initial user-packet validation
            validate.validateInitialOutboundPacket(packet, this.config.protocolVersion);
            (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Submitting operation ".concat(operation.id, " of type ").concat(mqtt5_packet.PacketType[operation.type]); });
            // don't want to log the packet until it passes initial validation
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return model.internalPacketToLogString(packet, "  "); });
            // offline queue policy check
            if (queueType == OperationQueueType.User) {
                if (this.state != ProtocolStateType.Connected) {
                    if (!this.operationPassesOfflineQueuePolicy(operation.id)) {
                        // gets failed properly in catch clause
                        throw new error_1.CrtError("User-submitted operation did not pass offline queue policy check");
                    }
                }
            }
            var queue = this.getOperationQueue(queueType);
            if (submitLocation == QueueEndType.Front) {
                (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Operation ".concat(operation.id, " enqueued to front of ").concat(OperationQueueType[queueType], " operation queue"); });
                queue.unshift(operation.id);
            }
            else {
                (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Operation ".concat(operation.id, " enqueued to back of ").concat(OperationQueueType[queueType], " operation queue"); });
                queue.push(operation.id);
            }
        }
        catch (e) {
            this.failOperation(operation.id, e);
        }
    };
    ProtocolState.prototype.createDefaultResultHandler = function () {
        var _this = this;
        return {
            onCompletionSuccess: function () { },
            onCompletionFailure: function (error) { _this.halt(HaltEventType.Unknown, error); }
        };
    };
    /**
     * Helper function to submit internal high-priority operations: connect, pingreq, puback
     */
    ProtocolState.prototype.submitOperationHighPriority = function (packet, queueEnd, resultHandler) {
        if (resultHandler === void 0) { resultHandler = this.createDefaultResultHandler(); }
        if (packet.type == undefined) {
            this.halt(HaltEventType.Unknown, new error_1.CrtError("Packet type not set"));
            return;
        }
        var operation = {
            type: packet.type,
            id: this.nextOperationId++,
            packet: model.convertInternalPacketToBinary(packet),
            options: {
                resultHandler: resultHandler
            },
            numAttempts: 0,
        };
        this.submitOperation(operation, packet, OperationQueueType.HighPriority, queueEnd);
    };
    ProtocolState.prototype.submitPublish = function (context) {
        var operation = {
            type: mqtt5_packet.PacketType.Publish,
            id: this.nextOperationId++,
            packet: model.convertPublishPacketToBinary(context.packet),
            options: context.options,
            numAttempts: 0,
        };
        this.submitOperation(operation, context.packet, OperationQueueType.User, QueueEndType.Back);
    };
    ProtocolState.prototype.submitSubscribe = function (context) {
        var operation = {
            type: mqtt5_packet.PacketType.Subscribe,
            id: this.nextOperationId++,
            packet: model.convertSubscribePacketToBinary(context.packet),
            options: context.options,
            numAttempts: 0,
        };
        this.submitOperation(operation, context.packet, OperationQueueType.User, QueueEndType.Back);
    };
    ProtocolState.prototype.submitUnsubscribe = function (context) {
        var operation = {
            type: mqtt5_packet.PacketType.Unsubscribe,
            id: this.nextOperationId++,
            packet: model.convertUnsubscribePacketToBinary(context.packet),
            options: context.options,
            numAttempts: 0,
        };
        this.submitOperation(operation, context.packet, OperationQueueType.User, QueueEndType.Back);
    };
    ProtocolState.prototype.submitDisconnect = function (context) {
        if (this.state != ProtocolStateType.Connected) {
            (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "Ignoring disconnect packet submission; not connected");
            return;
        }
        var disconnectOptions = {
            resultHandler: context.resultHandler
        };
        var operation = {
            type: mqtt5_packet.PacketType.Disconnect,
            id: this.nextOperationId++,
            packet: model.convertDisconnectPacketToBinary(context.packet),
            options: disconnectOptions,
            numAttempts: 0,
        };
        // disconnects go to the front of the high priority queue
        this.submitOperation(operation, context.packet, OperationQueueType.HighPriority, QueueEndType.Front);
    };
    ProtocolState.prototype.handleConnectionOpened = function (context) {
        (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "handleConnectionOpened called");
        if (this.state != ProtocolStateType.Disconnected) {
            throw new error_1.CrtError("Connection opened while not disconnected");
        }
        this.haltState = undefined;
        this.changeState(ProtocolStateType.PendingConnack);
        this.encoder.reset();
        this.decoder.reset();
        this.pendingConnackTimeoutElapsedMillis = context.establishmentTimeoutMillis;
        var connect = this.buildConnectPacket();
        this.lastOutboundConnect = connect;
        this.submitOperationHighPriority(connect, QueueEndType.Front);
    };
    ProtocolState.prototype.handleConnectionClosed = function () {
        var _this = this;
        (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "handleConnectionClosed called");
        if (this.state == ProtocolStateType.Disconnected) {
            throw new error_1.CrtError("Connection closed while disconnected");
        }
        this.changeState(ProtocolStateType.Disconnected);
        this.requeueCurrentOperation();
        this.pendingWriteCompletion = false;
        this.pendingFlushOperations = [];
        this.operationTimeouts.clear();
        this.nextOutboundPingElapsedMillis = undefined;
        this.pendingPingrespTimeoutElapsedMillis = undefined;
        this.pendingConnackTimeoutElapsedMillis = undefined;
        this.pendingPublishAcknowledgements.clear();
        this.pendingPublishAcknowledgementsByPacketId.clear();
        var failError = new error_1.CrtError("failed OfflineQueuePolicy check on disconnect");
        // 0. fail high priority queue (disconnect, connect, pingreq, puback); if we ever support qos 2 this changes
        this.highPriorityOperationQueue.forEach(function (operationId) { _this.failOperation(operationId, failError); });
        this.highPriorityOperationQueue = [];
        // 1. filter write completions, keep survivors
        var remainingWriteCompletions = this.partitionAndFailQueueByOfflineQueuePolicy(this.pendingWriteCompletionOperations, failError);
        this.pendingWriteCompletionOperations = [];
        // 2. process pending acks:
        //   (a) mark publish pending ack as duplicate and append to resubmit queue
        //   (b) filter non-publish pending ack and append to resubmit queue
        this.pendingAcks.forEach(function (operationId) {
            var operation = _this.operations.get(operationId);
            if (operation) {
                if (operation.type == mqtt5_packet.PacketType.Publish) {
                    var publish = operation.packet;
                    publish.duplicate = 1;
                    _this.resubmitOperationQueue.push(operationId);
                }
                else if (_this.operationPassesOfflineQueuePolicy(operationId)) {
                    _this.resubmitOperationQueue.push(operationId);
                }
                else {
                    _this.failOperation(operationId, failError);
                }
            }
        });
        // 3. filter user queue
        this.userOperationQueue = this.partitionAndFailQueueByOfflineQueuePolicy(this.userOperationQueue, failError);
        // 4. append preserved write completion operations to resubmit queue
        this.resubmitOperationQueue = this.resubmitOperationQueue.concat(remainingWriteCompletions);
        // side-affected queues (resubmit) will be sorted on the transition to connected state
    };
    ProtocolState.prototype.isReceivedPacketTypeValidForState = function (type) {
        switch (this.state) {
            case ProtocolStateType.Connected:
                if (type == mqtt5_packet.PacketType.Publish ||
                    type == mqtt5_packet.PacketType.Puback ||
                    type == mqtt5_packet.PacketType.Suback ||
                    type == mqtt5_packet.PacketType.Unsuback ||
                    type == mqtt5_packet.PacketType.Pingresp) {
                    return true;
                }
                if (type == mqtt5_packet.PacketType.Disconnect) {
                    return this.config.protocolVersion == mqtt_shared.ProtocolMode.Mqtt5;
                }
                return false;
            case ProtocolStateType.PendingConnack:
                return type == mqtt5_packet.PacketType.Connack;
            case ProtocolStateType.Disconnected:
                return false;
            default:
                throw new error_1.CrtError("Unknown protocol state");
        }
    };
    ProtocolState.prototype.handleIncomingData = function (context) {
        var e_1, _a;
        (0, io_1.logInfo)(PROTOCOL_STATE_LOG_SUBJECT, "handleIncomingData called");
        if (this.haltState) {
            return;
        }
        if (this.state == ProtocolStateType.Disconnected) {
            this.halt(HaltEventType.Unknown, new error_1.CrtError("Data received while disconnected"));
            return;
        }
        var incomingDataContext = context.context;
        var packets = this.decoder.decode(incomingDataContext.data);
        var _loop_3 = function (packet) {
            if (!this_3.isReceivedPacketTypeValidForState(packet.type)) {
                this_3.halt(HaltEventType.ProtocolError, new error_1.CrtError("Received packet type (".concat(packet.type, ") not valid for current state")));
                return { value: void 0 };
            }
            try {
                validate.validateInboundPacket(packet, this_3.config.protocolVersion);
            }
            catch (e) {
                (0, io_1.flogError)(PROTOCOL_STATE_LOG_SUBJECT, function () { var _a; return "Invalid packet of type ".concat(mqtt5_packet.PacketType[(_a = packet.type) !== null && _a !== void 0 ? _a : -1], " received"); });
                this_3.halt(HaltEventType.ProtocolError, e);
                return { value: void 0 };
            }
            this_3.handleIncomingPacket(packet);
        };
        var this_3 = this;
        try {
            for (var packets_1 = __values(packets), packets_1_1 = packets_1.next(); !packets_1_1.done; packets_1_1 = packets_1.next()) {
                var packet = packets_1_1.value;
                var state_2 = _loop_3(packet);
                if (typeof state_2 === "object")
                    return state_2.value;
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (packets_1_1 && !packets_1_1.done && (_a = packets_1.return)) _a.call(packets_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    ProtocolState.prototype.handleIncomingPacket = function (packet) {
        // at this point we can assume the packet is valid for the current state
        (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { var _a; return "Received packet of type ".concat(mqtt5_packet.PacketType[(_a = packet.type) !== null && _a !== void 0 ? _a : -1], ":"); });
        (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return model.internalPacketToLogString(packet, "  "); });
        switch (packet.type) {
            case mqtt5_packet.PacketType.Connack:
                this.handleIncomingConnack(packet);
                break;
            case mqtt5_packet.PacketType.Publish:
                this.handleIncomingPublish(packet);
                break;
            case mqtt5_packet.PacketType.Puback:
                this.handleIncomingPuback(packet);
                break;
            case mqtt5_packet.PacketType.Suback:
                this.handleIncomingSuback(packet);
                break;
            case mqtt5_packet.PacketType.Unsuback:
                this.handleIncomingUnsuback(packet);
                break;
            case mqtt5_packet.PacketType.Disconnect:
                this.handleIncomingDisconnect(packet);
                break;
            case mqtt5_packet.PacketType.Pingresp:
                this.handleIncomingPingresp();
                break;
            default:
                throw new error_1.CrtError("Unexpected incoming packet type");
        }
    };
    ProtocolState.prototype.handleIncomingConnack = function (packet) {
        this.pendingConnackTimeoutElapsedMillis = undefined;
        this.emit(ProtocolState.CONNACK_RECEIVED, {
            packet: packet
        });
        if (packet.reasonCode == mqtt5_packet.ConnectReasonCode.Success) {
            this.hasSuccessfullyConnected = true;
            this.changeState(ProtocolStateType.Connected);
            this.lastNegotiatedSettings = createNegotiatedSettings(this.lastOutboundConnect, packet);
            this.resetNextPing();
            this.applySessionState(packet.sessionPresent);
        }
        else {
            this.halt(HaltEventType.Normal, new error_1.CrtError("Connection rejected with reason code ".concat(packet.reasonCode)));
        }
    };
    ProtocolState.prototype.acquirePublishAcknowledgementControlId = function (packet) {
        if (packet.qos != mqtt5_packet.QoS.AtMostOnce && packet.packetId) {
            var existingAckControlId = this.pendingPublishAcknowledgementsByPacketId.get(packet.packetId);
            if (existingAckControlId != undefined) {
                return existingAckControlId;
            }
            var controlId = this.nextPublishAcknowledgementControlId++;
            this.pendingPublishAcknowledgements.set(controlId, {
                packetId: packet.packetId,
                qos: packet.qos
            });
            this.pendingPublishAcknowledgementsByPacketId.set(packet.packetId, controlId);
            return controlId;
        }
        return undefined;
    };
    ProtocolState.prototype.submitAcknowledgement = function (controlId) {
        var entry = this.pendingPublishAcknowledgements.get(controlId);
        if (!entry) {
            return;
        }
        if (entry.qos == mqtt5_packet.QoS.AtLeastOnce) {
            var puback = {
                type: mqtt5_packet.PacketType.Puback,
                packetId: entry.packetId,
                reasonCode: mqtt5_packet.PubackReasonCode.Success
            };
            this.submitOperationHighPriority(puback, QueueEndType.Back);
        }
        this.pendingPublishAcknowledgements.delete(controlId);
        this.pendingPublishAcknowledgementsByPacketId.delete(entry.packetId);
    };
    ProtocolState.prototype.handleIncomingPublish = function (packet) {
        var acknowledgementControl = undefined;
        var controlId = this.acquirePublishAcknowledgementControlId(packet);
        if (controlId) {
            var acknowledgementFunction = this.submitAcknowledgement.bind(this, controlId);
            acknowledgementControl = new mqtt_shared.PublishAcknowledgementHandleWrapper(new mqtt_shared.PublishAcknowledgementHandle(acknowledgementFunction));
        }
        var event = {
            packet: packet
        };
        mqtt_shared.emitAcknowledgeableEvent(this, ProtocolState.PUBLISH_RECEIVED, event, 'acknowledgementControl', acknowledgementControl);
    };
    ProtocolState.prototype.handleIncomingPuback = function (packet) {
        var id = packet.packetId;
        var operationId = this.pendingAcks.get(id);
        if (!operationId) {
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Puback with packet id ".concat(id, " received with no corresponding pending QoS1+ publish"); });
            return;
        }
        var operation = this.operations.get(operationId);
        if (!operation) {
            (0, io_1.flogError)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Puback with packet id ".concat(id, " corresponding to operation ").concat(operationId, " has no operation entry"); });
            return;
        }
        if (operation.type != mqtt5_packet.PacketType.Publish) {
            (0, io_1.flogWarn)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Puback with packet id ".concat(id, " corresponding to operation ").concat(operationId, " has operation that is not a Publish.  This is usually because of a preceding operation timeout conflicting with packet id reuse."); });
            return;
        }
        this.pushOutNextPing(operation.flushTimepoint);
        this.completeOperation(operationId, {
            type: PublishResultType.Qos1,
            packet: packet,
        });
    };
    ProtocolState.prototype.handleIncomingSuback = function (packet) {
        var id = packet.packetId;
        var operationId = this.pendingAcks.get(id);
        if (!operationId) {
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Suback with packet id ".concat(id, " received with no corresponding pending subscribe"); });
            return;
        }
        var operation = this.operations.get(operationId);
        if (!operation) {
            (0, io_1.flogError)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Suback with packet id ".concat(id, " corresponding to operation ").concat(operationId, " has no operation entry"); });
            return;
        }
        if (operation.type != mqtt5_packet.PacketType.Subscribe) {
            (0, io_1.flogWarn)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Suback with packet id ".concat(id, " corresponding to operation ").concat(operationId, " has operation that is not a Subscribe.  This is usually because of a preceding operation timeout conflicting with packet id reuse."); });
            return;
        }
        this.pushOutNextPing(operation.flushTimepoint);
        this.completeOperation(operationId, packet);
    };
    ProtocolState.prototype.handleIncomingUnsuback = function (packet) {
        var id = packet.packetId;
        var operationId = this.pendingAcks.get(id);
        if (!operationId) {
            (0, io_1.flogDebug)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Unsuback with packet id ".concat(id, " received with no corresponding pending unsubscribe"); });
            return;
        }
        var operation = this.operations.get(operationId);
        if (!operation) {
            (0, io_1.flogError)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Unsuback with packet id ".concat(id, " corresponding to operation ").concat(operationId, " has no operation entry"); });
            return;
        }
        if (operation.type != mqtt5_packet.PacketType.Unsubscribe) {
            (0, io_1.flogWarn)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Unsuback with packet id ".concat(id, " corresponding to operation ").concat(operationId, " has operation that is not an Unsubscribe.  This is usually because of a preceding operation timeout conflicting with packet id reuse."); });
            return;
        }
        this.pushOutNextPing(operation.flushTimepoint);
        this.completeOperation(operationId, packet);
    };
    ProtocolState.prototype.handleIncomingDisconnect = function (packet) {
        this.emit(ProtocolState.DISCONNECT_RECEIVED, {
            packet: packet
        });
        this.halt(HaltEventType.Normal, new error_1.CrtError("Server-side disconnect"));
    };
    ProtocolState.prototype.handleIncomingPingresp = function () {
        this.pendingPingrespTimeoutElapsedMillis = undefined;
    };
    ProtocolState.prototype.handleWriteCompletion = function () {
        var _this = this;
        (0, io_1.logDebug)(PROTOCOL_STATE_LOG_SUBJECT, "handleWriteCompletion called");
        if (this.haltState) {
            return;
        }
        if (this.state == ProtocolStateType.Disconnected) {
            this.halt(HaltEventType.Unknown, new error_1.CrtError("Received write completion while disconnected"));
            return;
        }
        if (!this.pendingWriteCompletion) {
            this.halt(HaltEventType.Unknown, new error_1.CrtError("Received write completion while no write was pending"));
            return;
        }
        this.pendingFlushOperations.forEach(function (id) {
            var operation = _this.operations.get(id);
            if (operation) {
                operation.flushTimepoint = _this.elapsedMillis;
            }
        });
        this.pendingFlushOperations = [];
        this.pendingWriteCompletionOperations.forEach(function (id) {
            _this.completeOperation(id, undefined);
        });
        this.pendingWriteCompletionOperations = [];
        this.pendingWriteCompletion = false;
    };
    /**
     * Helper function to determine if the clean start flag should be set on the client's Connect packet
     */
    ProtocolState.prototype.computeCleanStart = function () {
        var _a;
        var resumeSessionPolicy = (_a = this.config.connectOptions.resumeSessionPolicy) !== null && _a !== void 0 ? _a : ResumeSessionPolicyType.Default;
        switch (resumeSessionPolicy) {
            case ResumeSessionPolicyType.Always:
                return false;
            case ResumeSessionPolicyType.PostSuccess:
                return !this.hasSuccessfullyConnected;
            default:
                return true;
        }
    };
    ProtocolState.prototype.buildConnectPacket = function () {
        var connectOptions = this.config.connectOptions;
        var connect_packet = {
            type: mqtt5_packet.PacketType.Connect,
            keepAliveIntervalSeconds: connectOptions.keepAliveIntervalSeconds,
            cleanStart: this.computeCleanStart()
        };
        if (connectOptions.clientId != undefined) {
            connect_packet.clientId = connectOptions.clientId;
        }
        if (connectOptions.username != undefined) {
            connect_packet.username = connectOptions.username;
        }
        if (connectOptions.password != undefined) {
            connect_packet.password = connectOptions.password;
        }
        if (connectOptions.sessionExpiryIntervalSeconds != undefined) {
            connect_packet.sessionExpiryIntervalSeconds = connectOptions.sessionExpiryIntervalSeconds;
        }
        if (connectOptions.requestResponseInformation != undefined) {
            connect_packet.requestResponseInformation = connectOptions.requestResponseInformation;
        }
        if (connectOptions.requestProblemInformation != undefined) {
            connect_packet.requestProblemInformation = connectOptions.requestProblemInformation;
        }
        if (connectOptions.receiveMaximum != undefined) {
            connect_packet.receiveMaximum = connectOptions.receiveMaximum;
        }
        if (connectOptions.maximumPacketSizeBytes != undefined) {
            connect_packet.maximumPacketSizeBytes = connectOptions.maximumPacketSizeBytes;
        }
        if (connectOptions.willDelayIntervalSeconds != undefined) {
            connect_packet.willDelayIntervalSeconds = connectOptions.willDelayIntervalSeconds;
        }
        if (connectOptions.will != undefined) {
            connect_packet.will = model.clonePublishShallow(connectOptions.will);
        }
        if (connectOptions.userProperties != undefined) {
            connect_packet.userProperties = connectOptions.userProperties;
        }
        if (connectOptions.connectPacketTransformer) {
            connectOptions.connectPacketTransformer(connect_packet);
        }
        return connect_packet;
    };
    ProtocolState.prototype.halt = function (type, error) {
        // first event takes precedence
        if (this.haltState) {
            return;
        }
        this.haltState = {
            type: type,
            reason: error
        };
        (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Halted with category ".concat(HaltEventType[type], " and reason \"").concat(error.toString(), "\""); });
        this.emit(ProtocolState.HALTED, this.haltState);
    };
    ProtocolState.prototype.requeueCurrentOperation = function () {
        var _this = this;
        if (this.currentOperation == undefined) {
            return;
        }
        var operation = this.operations.get(this.currentOperation);
        if (operation == undefined) {
            return;
        }
        if (isUserOperationType(operation.type)) {
            // qos 1+ publishes that have been sent at least once must go in the resubmit queue
            var resubmit = operation.numAttempts > 0;
            if (resubmit && operation.type == mqtt5_packet_1.PacketType.Publish) {
                var publish = operation.packet;
                if (publish.qos == mqtt5_packet.QoS.AtMostOnce) {
                    resubmit = false;
                }
            }
            if (resubmit) {
                (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Requeuing current operation ".concat(_this.currentOperation, " to Resubmit operation queue"); });
                this.resubmitOperationQueue.unshift(this.currentOperation);
            }
            else {
                (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Requeuing current operation ".concat(_this.currentOperation, " to User operation queue"); });
                this.userOperationQueue.unshift(this.currentOperation);
            }
        }
        else {
            (0, io_1.flogInfo)(PROTOCOL_STATE_LOG_SUBJECT, function () { return "Requeuing current operation ".concat(_this.currentOperation, " to HighPriority operation queue"); });
            this.highPriorityOperationQueue.unshift(this.currentOperation);
        }
        this.currentOperation = undefined;
    };
    ProtocolState.prototype.operationPassesOfflineQueuePolicy = function (operationId) {
        var operation = this.operations.get(operationId);
        if (!operation) {
            return false;
        }
        var queuePolicy = this.config.offlineQueuePolicy;
        if (queuePolicy == OfflineQueuePolicy.PreserveNothing) {
            return false;
        }
        switch (operation.type) {
            case mqtt5_packet.PacketType.Publish:
                if (queuePolicy == OfflineQueuePolicy.PreserveAll) {
                    return true;
                }
                var publish = operation.packet;
                return publish.qos != mqtt5_packet.QoS.AtMostOnce;
            case mqtt5_packet.PacketType.Subscribe:
            case mqtt5_packet.PacketType.Unsubscribe:
                return queuePolicy == OfflineQueuePolicy.PreserveAll || queuePolicy == OfflineQueuePolicy.PreserveAcknowledged;
            default:
                return false;
        }
    };
    ProtocolState.prototype.partitionAndFailQueueByOfflineQueuePolicy = function (queue, failError) {
        var _this = this;
        var preserved = [];
        queue.forEach(function (operationId) {
            if (_this.operationPassesOfflineQueuePolicy(operationId)) {
                preserved.push(operationId);
            }
            else {
                _this.failOperation(operationId, failError);
            }
        });
        return preserved;
    };
    ProtocolState.prototype.applySessionState = function (sessionPresent) {
        var _this = this;
        if (!sessionPresent) {
            var failError = new error_1.CrtError("failed OfflineQueuePolicy check on reconnect with no session");
            var remaining = this.partitionAndFailQueueByOfflineQueuePolicy(this.resubmitOperationQueue, failError);
            this.userOperationQueue = this.userOperationQueue.concat(remaining);
            this.resubmitOperationQueue = [];
            // undo all packet id bindings
            this.boundPacketIds.forEach(function (operationId, packetId) {
                var operation = _this.operations.get(operationId);
                if (operation) {
                    operation.packetId = undefined;
                }
            });
            this.boundPacketIds.clear();
            this.nextPacketId = 1;
        }
        this.sortOperationQueue(this.userOperationQueue);
        this.sortOperationQueue(this.resubmitOperationQueue);
    };
    ProtocolState.prototype.sortOperationQueue = function (queue) {
        queue.sort(function (lhs, rhs) {
            if (lhs < rhs) {
                return -1;
            }
            else if (lhs > rhs) {
                return 1;
            }
            return 0;
        });
    };
    /**
     * Event emitted when the protocol object becomes halted.
     *
     * Listener type: {@link HaltedEventListener}
     *
     * @event
     */
    ProtocolState.HALTED = 'halted';
    /**
     * Event emitted when a disconnect packet is received
     *
     * Listener type: {@link DisconnectReceivedEventListener}
     *
     * @event
     */
    ProtocolState.DISCONNECT_RECEIVED = 'disconnectReceived';
    /**
     * Event emitted when a publish packet is received
     *
     * Listener type: {@link PublishReceivedEventListener}
     *
     * @event
     */
    ProtocolState.PUBLISH_RECEIVED = 'publishReceived';
    /**
     * Event emitted when a connack packet is received
     *
     * Listener type: {@link ConnackReceivedEventListener}
     *
     * @event
     */
    ProtocolState.CONNACK_RECEIVED = 'connackReceived';
    return ProtocolState;
}(event_1.BufferedEventEmitter));
exports.ProtocolState = ProtocolState;
/* Negotiated settings helpers */
function createNegotiatedSettings(connect, connack) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o;
    return {
        maximumQos: Math.min((_a = connack.maximumQos) !== null && _a !== void 0 ? _a : mqtt5.QoS.ExactlyOnce, mqtt5.QoS.AtLeastOnce),
        sessionExpiryInterval: (_c = (_b = connack.sessionExpiryInterval) !== null && _b !== void 0 ? _b : connect.sessionExpiryIntervalSeconds) !== null && _c !== void 0 ? _c : 0,
        receiveMaximumFromServer: (_d = connack.receiveMaximum) !== null && _d !== void 0 ? _d : mqtt_shared.DEFAULT_RECEIVE_MAXIMUM,
        maximumPacketSizeToServer: (_e = connack.maximumPacketSize) !== null && _e !== void 0 ? _e : mqtt_shared.MAXIMUM_PACKET_SIZE,
        topicAliasMaximumToServer: 0,
        topicAliasMaximumToClient: 0,
        serverKeepAlive: (_g = (_f = connack.serverKeepAlive) !== null && _f !== void 0 ? _f : connect.keepAliveIntervalSeconds) !== null && _g !== void 0 ? _g : mqtt_shared.DEFAULT_KEEP_ALIVE,
        retainAvailable: (_h = connack.retainAvailable) !== null && _h !== void 0 ? _h : true,
        wildcardSubscriptionsAvailable: (_j = connack.wildcardSubscriptionsAvailable) !== null && _j !== void 0 ? _j : true,
        subscriptionIdentifiersAvailable: (_k = connack.subscriptionIdentifiersAvailable) !== null && _k !== void 0 ? _k : true,
        sharedSubscriptionsAvailable: (_l = connack.sharedSubscriptionsAvailable) !== null && _l !== void 0 ? _l : true,
        rejoinedSession: connack.sessionPresent,
        clientId: (_o = (_m = connack.assignedClientIdentifier) !== null && _m !== void 0 ? _m : connect.clientId) !== null && _o !== void 0 ? _o : ""
    };
}
function createDefaultNegotiatedSettings() {
    return {
        maximumQos: mqtt5.QoS.AtLeastOnce,
        sessionExpiryInterval: 0,
        receiveMaximumFromServer: mqtt_shared.DEFAULT_RECEIVE_MAXIMUM,
        maximumPacketSizeToServer: mqtt_shared.MAXIMUM_PACKET_SIZE,
        topicAliasMaximumToServer: 0,
        topicAliasMaximumToClient: 0,
        serverKeepAlive: mqtt_shared.DEFAULT_KEEP_ALIVE,
        retainAvailable: true,
        wildcardSubscriptionsAvailable: true,
        subscriptionIdentifiersAvailable: true,
        sharedSubscriptionsAvailable: true,
        rejoinedSession: false,
        clientId: ""
    };
}
function createDefaultConnect() {
    return {
        keepAliveIntervalSeconds: mqtt_shared.DEFAULT_KEEP_ALIVE,
        cleanStart: true
    };
}
function foldTimeMin(lhs, rhs) {
    if (lhs == undefined) {
        return rhs;
    }
    if (rhs == undefined) {
        return lhs;
    }
    return Math.min(lhs, rhs);
}
exports.foldTimeMin = foldTimeMin;
function foldTimeMax(lhs, rhs) {
    if (lhs == undefined) {
        return rhs;
    }
    if (rhs == undefined) {
        return lhs;
    }
    return Math.max(lhs, rhs);
}
exports.foldTimeMax = foldTimeMax;
//# sourceMappingURL=protocol.js.map