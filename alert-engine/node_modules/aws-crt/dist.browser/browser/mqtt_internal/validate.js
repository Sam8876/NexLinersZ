"use strict";
/*
 * Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateOptionalBinaryData = exports.validateUserProperties = exports.validateOptionalString = exports.validateOptionalPositiveU32 = exports.validateOptionalUnsignedInteger = exports.validateUnsignedInteger = exports.validateOptionalU32 = exports.validateU16 = exports.validateInboundPacket = exports.validateBinaryOutboundPacket = exports.validateInitialOutboundPacket = void 0;
var error_1 = require("../error");
var encoder = __importStar(require("./encoder"));
var mqtt5_packet = __importStar(require("../../common/mqtt5_packet"));
var mqtt_shared = __importStar(require("../../common/mqtt_shared"));
var mqtt_shared_browser = __importStar(require("../mqtt_shared_browser"));
var model = __importStar(require("./model"));
/**
 * This module contains three validation suites needed by the client implementation:
 *
 * 1. Validation of user-submitted outbound packets at submission time.  This checks all public fields for valid
 *    types, values, and ranges.
 * 2. Validation of internal outbound packets prior to encoding.  This checks constraints on internally managed fields
 *    like packetId.  It also checks constraints that can change across separate connection (maximum qos, for example).
 * 3. Protocol-error validation of inbound packets from the server.  This primarily checks protocol constraints
 *    and enumerated values.  We don't validate against negotiated settings because the server breaking that contract
 *    isn't really a fatal flaw.  In that case, it's better to be forgiving.
 *
 * Validation differs slightly based on what protocol level is being used (primarily reason codes).  It is not
 * considered a validation error to have 5-only fields set while operating in 311; the additional fields are just
 * ignored.
 *
 * Validation failure is indicated by throwing a CrtError.
 *
 * There are a few validation checks that are done outside this module.  Validation of binary properties that
 * are lost on decoding (certain bits not set) is done in the decoder.  Validation of maximum packet size constraints
 * is done in the encoder (NYI though).
 *
 * We also skip validation of certain fields that are not relevant in the particular use case.  For example, we don't
 * validate subscription identifiers on outbound publishes because that field is ignored when converting the
 * submitted packet into the internal model.
 */
function validateInitialOutboundPacket(packet, mode) {
    switch (packet.type) {
        case mqtt5_packet.PacketType.Publish:
            validateUserSubmittedPublish(packet, mode);
            break;
        case mqtt5_packet.PacketType.Subscribe:
            validateUserSubmittedSubscribe(packet, mode);
            break;
        case mqtt5_packet.PacketType.Unsubscribe:
            validateUserSubmittedUnsubscribe(packet, mode);
            break;
        case mqtt5_packet.PacketType.Disconnect:
            validateUserSubmittedDisconnect(packet, mode);
            break;
        default:
            break;
    }
}
exports.validateInitialOutboundPacket = validateInitialOutboundPacket;
function validateBinaryOutboundPacket(packet, mode, settings) {
    switch (packet.type) {
        case mqtt5_packet.PacketType.Publish:
            validateBinaryPublish(packet, mode, settings, false);
            break;
        case mqtt5_packet.PacketType.Subscribe:
            validateBinarySubscribe(packet, mode, settings);
            break;
        case mqtt5_packet.PacketType.Unsubscribe:
            validateBinaryUnsubscribe(packet, mode, settings);
            break;
        case mqtt5_packet.PacketType.Disconnect:
            validateBinaryDisconnect(packet, mode, settings);
            break;
        case mqtt5_packet.PacketType.Connect:
            validateBinaryConnect(packet, mode, settings);
            break;
        case mqtt5_packet.PacketType.Puback:
            validateBinaryPuback(packet, mode, settings);
            break;
        default:
            break;
    }
}
exports.validateBinaryOutboundPacket = validateBinaryOutboundPacket;
function validateInboundPacket(packet, mode) {
    switch (packet.type) {
        case mqtt5_packet.PacketType.Publish:
            validateInboundPublish(packet, mode);
            break;
        case mqtt5_packet.PacketType.Puback:
            validateInboundPuback(packet, mode);
            break;
        case mqtt5_packet.PacketType.Connack:
            validateInboundConnack(packet, mode);
            break;
        case mqtt5_packet.PacketType.Suback:
            validateInboundSuback(packet, mode);
            break;
        case mqtt5_packet.PacketType.Unsuback:
            validateInboundUnsuback(packet, mode);
            break;
        case mqtt5_packet.PacketType.Disconnect:
            validateInboundDisconnect(packet, mode);
            break;
        default:
            break;
    }
}
exports.validateInboundPacket = validateInboundPacket;
// primitive fields
// we don't validate booleans because we use truthiness to map to 0 or 1, so there's no need
function validateU8(value, fieldName) {
    if (!Number.isInteger(value) || value < 0 || value > 255) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" is not a valid u8"));
    }
}
function validateU16(value, fieldName) {
    if (!Number.isInteger(value) || value < 0 || value > 65535) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" is not a valid u16"));
    }
}
exports.validateU16 = validateU16;
function validateOptionalPositiveU16(value, fieldName) {
    if (value != undefined) {
        validateU16(value, fieldName);
        if (value == 0) {
            throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" cannot be 0"));
        }
    }
}
function validateU32(value, fieldName) {
    if (!Number.isInteger(value) || value < 0 || value > (256 * 256 * 256 * 256 - 1)) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" is not a valid u32"));
    }
}
function validateOptionalU32(value, fieldName) {
    if (value != undefined) {
        validateU32(value, fieldName);
    }
}
exports.validateOptionalU32 = validateOptionalU32;
function validateVli(value, fieldName) {
    if (!Number.isInteger(value) || value < 0 || value > (128 * 128 * 128 * 128 - 1)) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" cannot be VLI-encoded"));
    }
}
function validateOptionalVli(value, fieldName) {
    if (value != undefined) {
        validateVli(value, fieldName);
    }
}
function validateUnsignedInteger(value, fieldName) {
    if (!Number.isInteger(value) || value < 0) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" is not a valid unsigned integer"));
    }
}
exports.validateUnsignedInteger = validateUnsignedInteger;
function validateOptionalUnsignedInteger(value, fieldName) {
    if (value === undefined) {
        return;
    }
    validateUnsignedInteger(value, fieldName);
}
exports.validateOptionalUnsignedInteger = validateOptionalUnsignedInteger;
function validateOptionalPositiveU32(value, fieldName) {
    if (value === undefined) {
        return;
    }
    if (!Number.isInteger(value) || value <= 0 || value > (256 * 256 * 256 * 256 - 1)) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" is not a valid positive 32 bit integer"));
    }
}
exports.validateOptionalPositiveU32 = validateOptionalPositiveU32;
// we don't validate length here because we don't know encoding length without doing a utf-8 conversion.
// This means we validate string length in the internal validators which checks ArrayBuffer lengths.
function validateString(value, fieldName) {
    if ((typeof value === 'string') || (value instanceof String)) {
        return;
    }
    throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(value, "\" is not a valid string"));
}
function validateOptionalString(value, fieldName) {
    if (value === undefined) {
        return;
    }
    validateString(value, fieldName);
}
exports.validateOptionalString = validateOptionalString;
function validateBufferLength(value, fieldName) {
    // we don't do typechecking here because this is only used by internal validators which are checking values
    // that we explicitly constructed ourselves when converting to the binary model
    if (value.byteLength > 65535) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" is not a 16-bit length buffer"));
    }
}
function validateOptionalBufferLength(value, fieldName) {
    if (value == undefined) {
        return;
    }
    validateBufferLength(value, fieldName);
}
// enum validation
function validateSubackReasonCode(value, mode) {
    validateU8(value, 'reasonCodes');
    if (mode == mqtt_shared.ProtocolMode.Mqtt311) {
        switch (value) {
            case mqtt5_packet.SubackReasonCode.GrantedQoS0:
            case mqtt5_packet.SubackReasonCode.GrantedQoS1:
            case mqtt5_packet.SubackReasonCode.GrantedQoS2:
            case mqtt5_packet.SubackReasonCode.Failure311:
                break;
            default:
                throw new error_1.CrtError("\"".concat(value, "\" is not a valid MQTT311 SubackReasonCode"));
        }
    }
    else if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        switch (value) {
            case mqtt5_packet.SubackReasonCode.GrantedQoS0:
            case mqtt5_packet.SubackReasonCode.GrantedQoS1:
            case mqtt5_packet.SubackReasonCode.GrantedQoS2:
            case mqtt5_packet.SubackReasonCode.UnspecifiedError:
            case mqtt5_packet.SubackReasonCode.ImplementationSpecificError:
            case mqtt5_packet.SubackReasonCode.NotAuthorized:
            case mqtt5_packet.SubackReasonCode.TopicFilterInvalid:
            case mqtt5_packet.SubackReasonCode.PacketIdentifierInUse:
            case mqtt5_packet.SubackReasonCode.QuotaExceeded:
            case mqtt5_packet.SubackReasonCode.SharedSubscriptionsNotSupported:
            case mqtt5_packet.SubackReasonCode.SubscriptionIdentifiersNotSupported:
            case mqtt5_packet.SubackReasonCode.WildcardSubscriptionsNotSupported:
                break;
            default:
                throw new error_1.CrtError("\"".concat(value, "\" is not a valid MQTT5 SubackReasonCode"));
        }
    }
}
function validatePubackReasonCode(value) {
    if (mqtt5_packet.PubackReasonCode[value] === undefined) {
        throw new error_1.CrtError("\"".concat(value, "\" is not a valid MQTT5 PubackReasonCode"));
    }
}
function validateUnsubackReasonCode(value, mode) {
    if (mode == mqtt_shared.ProtocolMode.Mqtt311) {
        return;
    }
    if (mqtt5_packet.UnsubackReasonCode[value] === undefined) {
        throw new error_1.CrtError("\"".concat(value, "\" is not a valid MQTT5 UnsubackReasonCode"));
    }
}
function validateConnectReasonCode(value, mode) {
    validateU8(value, 'reasonCode');
    if (mode == mqtt_shared.ProtocolMode.Mqtt311) {
        switch (value) {
            case mqtt5_packet.ConnectReasonCode.Success:
            case mqtt5_packet.ConnectReasonCode.UnacceptableProtocolVersion311:
            case mqtt5_packet.ConnectReasonCode.ClientIdRejected311:
            case mqtt5_packet.ConnectReasonCode.ServerUnavailable311:
            case mqtt5_packet.ConnectReasonCode.InvalidUsernameOrPassword311:
            case mqtt5_packet.ConnectReasonCode.NotAuthorized311:
                break;
            default:
                throw new error_1.CrtError("\"".concat(value, "\" is not a valid MQTT311 ConnectReasonCode"));
        }
    }
    else if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        switch (value) {
            case mqtt5_packet.ConnectReasonCode.Success:
            case mqtt5_packet.ConnectReasonCode.UnspecifiedError:
            case mqtt5_packet.ConnectReasonCode.MalformedPacket:
            case mqtt5_packet.ConnectReasonCode.ProtocolError:
            case mqtt5_packet.ConnectReasonCode.ImplementationSpecificError:
            case mqtt5_packet.ConnectReasonCode.UnsupportedProtocolVersion:
            case mqtt5_packet.ConnectReasonCode.ClientIdentifierNotValid:
            case mqtt5_packet.ConnectReasonCode.BadUsernameOrPassword:
            case mqtt5_packet.ConnectReasonCode.NotAuthorized:
            case mqtt5_packet.ConnectReasonCode.ServerUnavailable:
            case mqtt5_packet.ConnectReasonCode.ServerBusy:
            case mqtt5_packet.ConnectReasonCode.Banned:
            case mqtt5_packet.ConnectReasonCode.BadAuthenticationMethod:
            case mqtt5_packet.ConnectReasonCode.TopicNameInvalid:
            case mqtt5_packet.ConnectReasonCode.PacketTooLarge:
            case mqtt5_packet.ConnectReasonCode.QuotaExceeded:
            case mqtt5_packet.ConnectReasonCode.PayloadFormatInvalid:
            case mqtt5_packet.ConnectReasonCode.RetainNotSupported:
            case mqtt5_packet.ConnectReasonCode.QosNotSupported:
            case mqtt5_packet.ConnectReasonCode.UseAnotherServer:
            case mqtt5_packet.ConnectReasonCode.ServerMoved:
            case mqtt5_packet.ConnectReasonCode.ConnectionRateExceeded:
                break;
            default:
                throw new error_1.CrtError("\"".concat(value, "\" is not a valid MQTT5 ConnectReasonCode"));
        }
    }
}
function validateDisconnectReasonCode(value, mode) {
    if (mode == mqtt_shared.ProtocolMode.Mqtt311) {
        return;
    }
    if (mqtt5_packet.DisconnectReasonCode[value] === undefined) {
        throw new error_1.CrtError("\"".concat(value, "\" is not a valid MQTT5 DisconnectReasonCode"));
    }
}
function validateQos(qos) {
    if (mqtt5_packet.QoS[qos] === undefined) {
        throw new error_1.CrtError("\"".concat(qos, "\" is not a valid QualityOfService"));
    }
}
function validateOptionalPayloadFormat(payloadFormat, fieldName) {
    if (payloadFormat === undefined) {
        return;
    }
    if (mqtt5_packet.PayloadFormatIndicator[payloadFormat] === undefined) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(payloadFormat, "\" is not a valid PayloadFormatIndicator"));
    }
}
function validateOptionalRetainHandlingType(retainHandling, fieldName) {
    if (retainHandling === undefined) {
        return;
    }
    if (mqtt5_packet.RetainHandlingType[retainHandling] === undefined) {
        throw new error_1.CrtError("Field \"".concat(fieldName, "\" with value \"").concat(retainHandling, "\" is not a valid RetainHandlingType"));
    }
}
// misc validation utilities
function validateUserProperties(userProperties) {
    var e_1, _a;
    if (!userProperties) {
        return;
    }
    if (!Array.isArray(userProperties)) {
        throw new error_1.CrtError('UserProperties is not an array');
    }
    try {
        for (var userProperties_1 = __values(userProperties), userProperties_1_1 = userProperties_1.next(); !userProperties_1_1.done; userProperties_1_1 = userProperties_1.next()) {
            var userProperty = userProperties_1_1.value;
            validateString(userProperty.name, 'UserProperty.name');
            validateString(userProperty.value, 'UserProperty.value');
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (userProperties_1_1 && !userProperties_1_1.done && (_a = userProperties_1.return)) _a.call(userProperties_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
}
exports.validateUserProperties = validateUserProperties;
function validatePayload(payload) {
    if (!payload) {
        return;
    }
    if (!model.isValidPayload(payload)) {
        throw new error_1.CrtError("Invalid payload value");
    }
}
function validateBinaryData(value, fieldName) {
    if (!model.isValidBinaryData(value)) {
        throw new error_1.CrtError("Field ".concat(fieldName, " is not valid binary data"));
    }
}
function validateOptionalBinaryData(value, fieldName) {
    if (!value) {
        return;
    }
    return validateBinaryData(value, fieldName);
}
exports.validateOptionalBinaryData = validateOptionalBinaryData;
function validateTopic(value, fieldName) {
    validateString(value, fieldName);
    if (!mqtt_shared_browser.isValidTopic(value)) {
        throw new error_1.CrtError("value \"".concat(value, "\" of field \"").concat(fieldName, "\" is not a valid topic"));
    }
}
function validateOptionalTopic(value, fieldName) {
    if (value == undefined) {
        return;
    }
    validateTopic(value, fieldName);
}
function validateTopicFilter(value, fieldName) {
    validateString(value, fieldName);
    if (!mqtt_shared_browser.isValidTopicFilter(value)) {
        throw new error_1.CrtError("value \"".concat(value, "\" of field \"").concat(fieldName, "\" is not a valid topic filter"));
    }
}
function validateRequiredPacketId(value, fieldName) {
    if (value == undefined) {
        throw new error_1.CrtError("packet id field ".concat(fieldName, " must be defined\""));
    }
    validateU16(value, fieldName);
    if (value == 0) {
        throw new error_1.CrtError("packet id field \"".concat(fieldName, "\" is not a valid packetId"));
    }
}
// user-submitted outbound packet validators
function validateUserSubmittedPublish(packet, mode) {
    validateTopic(packet.topicName, 'topicName');
    validatePayload(packet.payload);
    validateQos(packet.qos);
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateOptionalPayloadFormat(packet.payloadFormat, "payloadFormat");
        validateOptionalU32(packet.messageExpiryIntervalSeconds, "messageExpiryIntervalSeconds");
        validateOptionalPositiveU16(packet.topicAlias, "topicAlias"); // 0 is also invalid
        validateOptionalTopic(packet.responseTopic, "responseTopic");
        validateOptionalBinaryData(packet.correlationData, "correlationData");
        validateOptionalString(packet.contentType, "contentType");
        validateUserProperties(packet.userProperties);
    }
}
function validateSubscriptions(subscriptions, mode) {
    var e_2, _a;
    if (!Array.isArray(subscriptions)) {
        throw new error_1.CrtError("Subscriptions must be an array");
    }
    if (subscriptions.length == 0) {
        throw new error_1.CrtError("Subscriptions cannot be empty");
    }
    try {
        for (var subscriptions_1 = __values(subscriptions), subscriptions_1_1 = subscriptions_1.next(); !subscriptions_1_1.done; subscriptions_1_1 = subscriptions_1.next()) {
            var subscription = subscriptions_1_1.value;
            validateTopicFilter(subscription.topicFilter, "topicFilter");
            validateQos(subscription.qos);
            if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
                // no need to validate noLocal or retainAsPublished booleans
                validateOptionalRetainHandlingType(subscription.retainHandlingType, "Subscription.retainHandling");
            }
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (subscriptions_1_1 && !subscriptions_1_1.done && (_a = subscriptions_1.return)) _a.call(subscriptions_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
}
function validateUserSubmittedSubscribe(packet, mode) {
    validateSubscriptions(packet.subscriptions, mode);
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateOptionalVli(packet.subscriptionIdentifier, "subscriptionIdentifier");
        validateUserProperties(packet.userProperties);
    }
}
function validateUserSubmittedUnsubscribe(packet, mode) {
    var e_3, _a;
    if (!packet.topicFilters || packet.topicFilters.length == 0) {
        throw new error_1.CrtError("TopicFilters cannot be empty");
    }
    try {
        for (var _b = __values(packet.topicFilters), _c = _b.next(); !_c.done; _c = _b.next()) {
            var filter = _c.value;
            validateTopicFilter(filter, "topicFilters");
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_3) throw e_3.error; }
    }
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateUserProperties(packet.userProperties);
    }
}
function validateUserSubmittedDisconnect(packet, mode) {
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateDisconnectReasonCode(packet.reasonCode, mode);
        validateOptionalU32(packet.sessionExpiryIntervalSeconds, "sessionExpiryIntervalSeconds");
        validateOptionalString(packet.reasonString, "reasonString");
        validateOptionalString(packet.serverReference, "serverReference");
        validateUserProperties(packet.userProperties);
    }
}
// binary outbound packet validators; user-submitted validation is not repeated here
function validatePacketLength(packet, mode, maximumPacketSize) {
    var length = encoder.computePacketEncodingLength(packet, mode);
    if (length > maximumPacketSize) {
        throw new error_1.CrtError("Packet with length ".concat(length, " exceeds established maximum packet size of ").concat(maximumPacketSize));
    }
}
function validateBinaryUserProperties(userProperties) {
    var e_4, _a;
    if (!userProperties) {
        return;
    }
    try {
        for (var userProperties_2 = __values(userProperties), userProperties_2_1 = userProperties_2.next(); !userProperties_2_1.done; userProperties_2_1 = userProperties_2.next()) {
            var userProperty = userProperties_2_1.value;
            validateBufferLength(userProperty.name, "UserProperty.name");
            validateBufferLength(userProperty.value, "UserProperty.value");
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (userProperties_2_1 && !userProperties_2_1.done && (_a = userProperties_2.return)) _a.call(userProperties_2);
        }
        finally { if (e_4) throw e_4.error; }
    }
}
function validateBinaryPublish(packet, mode, settings, isWill) {
    if (isWill) {
        // topic will be checked below
        if (packet.payload && packet.payload.byteLength > 65535) {
            throw new error_1.CrtError("will payload too long");
        }
    }
    else {
        validatePacketLength(packet, mode, settings.maximumPacketSizeToServer);
    }
    if (!isWill) {
        if (packet.qos == mqtt5_packet.QoS.AtMostOnce) {
            if (packet.packetId != undefined) {
                throw new error_1.CrtError("packetId must not be set on outbound publish packets with QoS 0");
            }
            if (packet.duplicate) {
                throw new error_1.CrtError("duplicate must not be set on outbound publish packets with QoS 0");
            }
        }
        else {
            validateRequiredPacketId(packet.packetId, "packetId");
        }
    }
    if (packet.retain && !settings.retainAvailable) {
        throw new error_1.CrtError("retain cannot be set on outbound publish packets if the server does not support retained messages");
    }
    if (packet.qos > settings.maximumQos) {
        throw new error_1.CrtError("QoS ".concat(packet.qos, " is greater than the maximum QoS (").concat(settings.maximumQos, ") supported by the server"));
    }
    validateBufferLength(packet.topicName, "topicName");
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        if (packet.subscriptionIdentifiers != undefined) {
            throw new error_1.CrtError("subscriptionIdentifiers may not be set on outbound publish packets");
        }
        if (!isWill && packet.topicAlias != undefined) {
            if (packet.topicAlias == 0) {
                throw new error_1.CrtError("topicAlias cannot be zero");
            }
            else if (packet.topicAlias > settings.topicAliasMaximumToServer) {
                throw new error_1.CrtError("topicAlias value ".concat(packet.topicAlias, " is greater than the maximum topic alias (").concat(settings.topicAliasMaximumToServer, ") supported by the server"));
            }
        }
        validateOptionalBufferLength(packet.responseTopic, "responseTopic");
        validateOptionalBufferLength(packet.correlationData, "correlationData");
        validateOptionalBufferLength(packet.contentType, "contentType");
        validateBinaryUserProperties(packet.userProperties);
    }
}
function validateBinaryPuback(packet, mode, settings) {
    validatePacketLength(packet, mode, settings.maximumPacketSizeToServer);
    validateRequiredPacketId(packet.packetId, "packetId");
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateOptionalBufferLength(packet.reasonString, "reasonString");
        validateBinaryUserProperties(packet.userProperties);
    }
}
function validateSubscription(subscription, mode, settings) {
    var properties = mqtt_shared_browser.computeTopicProperties(subscription.topicFilterAsString, true);
    if (properties.isShared) {
        if (!settings.sharedSubscriptionsAvailable) {
            throw new error_1.CrtError("Shared subscriptions are not supported by the server");
        }
        if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
            if (subscription.noLocal) {
                throw new error_1.CrtError("noLocal may not be set on a shared subscriptions");
            }
        }
    }
    if (properties.hasWildcard && !settings.wildcardSubscriptionsAvailable) {
        throw new error_1.CrtError("Wildcard subscriptions are not supported by the server");
    }
    validateBufferLength(subscription.topicFilter, "subscription.topicFilter");
}
function validateBinarySubscribe(packet, mode, settings) {
    var e_5, _a;
    validatePacketLength(packet, mode, settings.maximumPacketSizeToServer);
    validateRequiredPacketId(packet.packetId, "packetId");
    try {
        for (var _b = __values(packet.subscriptions), _c = _b.next(); !_c.done; _c = _b.next()) {
            var subscription = _c.value;
            validateSubscription(subscription, mode, settings);
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_5) throw e_5.error; }
    }
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateBinaryUserProperties(packet.userProperties);
    }
}
function validateBinaryUnsubscribe(packet, mode, settings) {
    var e_6, _a;
    validatePacketLength(packet, mode, settings.maximumPacketSizeToServer);
    validateRequiredPacketId(packet.packetId, "packetId");
    try {
        for (var _b = __values(packet.topicFilters), _c = _b.next(); !_c.done; _c = _b.next()) {
            var topicFilter = _c.value;
            validateBufferLength(topicFilter, "topicFilter");
        }
    }
    catch (e_6_1) { e_6 = { error: e_6_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_6) throw e_6.error; }
    }
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateBinaryUserProperties(packet.userProperties);
    }
}
function validateBinaryDisconnect(packet, mode, settings) {
    validatePacketLength(packet, mode, settings.maximumPacketSizeToServer);
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        if (settings.sessionExpiryInterval == 0) {
            if (packet.sessionExpiryIntervalSeconds != undefined && packet.sessionExpiryIntervalSeconds > 0) {
                throw new error_1.CrtError("sessionExpiryIntervalSeconds cannot be positive when the connection was established with a zero-valued session expiry interval");
            }
        }
        validateOptionalBufferLength(packet.reasonString, "reasonString");
        validateOptionalBufferLength(packet.serverReference, "serverReference");
        validateBinaryUserProperties(packet.userProperties);
    }
}
// Connect packets are synthesized internally based on configuration settings and state
// we validate type and integer widths when we validate the corresponding component of client configuration
function validateBinaryConnect(packet, mode, settings) {
    validatePacketLength(packet, mode, settings.maximumPacketSizeToServer);
    validateOptionalBufferLength(packet.clientId, "clientId");
    validateOptionalBufferLength(packet.username, "username");
    validateOptionalBufferLength(packet.password, "password");
    if (packet.will) {
        validateBinaryPublish(packet.will, mode, settings, true);
    }
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        validateOptionalBufferLength(packet.authenticationMethod, "authenticationMethod");
        validateOptionalBufferLength(packet.authenticationData, "authenticationData");
        validateBinaryUserProperties(packet.userProperties);
    }
}
// inbound packet validators - we don't type check or integer-width check anything because we're the ones
// who initialized the packet with appropriate byte-level decoding operations.  We do check
//   1. enum values
//   2. packet ids (non-zero)
//   3. misc. property constraints
function validateInboundPublish(packet, mode) {
    validateQos(packet.qos);
    if (packet.qos == mqtt5_packet.QoS.AtMostOnce) {
        if (packet.packetId != undefined) {
            throw new error_1.CrtError("packetId must not be set on QoS 0 publishes");
        }
    }
    else {
        validateRequiredPacketId(packet.packetId, "packetId");
    }
    if (packet.topicName.length == 0) {
        throw new error_1.CrtError("topicName is empty (alias could not be resolved)");
    }
}
function validateInboundPuback(packet, mode) {
    validateRequiredPacketId(packet.packetId, "packetId");
    validatePubackReasonCode(packet.reasonCode);
}
function validateInboundConnack(packet, mode) {
    validateConnectReasonCode(packet.reasonCode, mode);
    if (packet.sessionPresent) {
        if (packet.reasonCode != mqtt5_packet.ConnectReasonCode.Success) {
            throw new error_1.CrtError("sessionPresent cannot be true with an unsuccessful connect reason code");
        }
    }
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        if (packet.receiveMaximum != undefined && packet.receiveMaximum == 0) {
            throw new error_1.CrtError("receiveMaximum must be a positive integer");
        }
        if (packet.maximumQos != undefined) {
            if (packet.maximumQos != mqtt5_packet.QoS.AtLeastOnce && packet.maximumQos != mqtt5_packet.QoS.AtMostOnce) {
                throw new error_1.CrtError("maximumQos can only be 0 or 1");
            }
        }
        if (packet.maximumPacketSize != undefined && packet.maximumPacketSize == 0) {
            throw new error_1.CrtError("maximumPacketSize must be a positive integer");
        }
    }
}
function validateInboundSuback(packet, mode) {
    var e_7, _a;
    validateRequiredPacketId(packet.packetId, "packetId");
    try {
        for (var _b = __values(packet.reasonCodes), _c = _b.next(); !_c.done; _c = _b.next()) {
            var reasonCode = _c.value;
            validateSubackReasonCode(reasonCode, mode);
        }
    }
    catch (e_7_1) { e_7 = { error: e_7_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_7) throw e_7.error; }
    }
}
function validateInboundUnsuback(packet, mode) {
    var e_8, _a;
    validateRequiredPacketId(packet.packetId, "packetId");
    try {
        for (var _b = __values(packet.reasonCodes), _c = _b.next(); !_c.done; _c = _b.next()) {
            var reasonCode = _c.value;
            validateUnsubackReasonCode(reasonCode, mode);
        }
    }
    catch (e_8_1) { e_8 = { error: e_8_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_8) throw e_8.error; }
    }
}
function validateInboundDisconnect(packet, mode) {
    validateDisconnectReasonCode(packet.reasonCode, mode);
    if (mode == mqtt_shared.ProtocolMode.Mqtt5) {
        if (packet.sessionExpiryIntervalSeconds != undefined) {
            throw new error_1.CrtError("server Disconnect packets must not define sessionExpiryIntervalSeconds");
        }
    }
}
//# sourceMappingURL=validate.js.map