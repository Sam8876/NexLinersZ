"use strict";
/*
 * Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PACKET_TYPE_FIRST_BYTE_PUBACK = exports.PACKET_TYPE_FIRST_BYTE_PUBLISH = exports.PACKET_TYPE_FIRST_BYTE_CONNACK = exports.PACKET_TYPE_FIRST_BYTE_CONNECT = exports.SUBSCRIPTION_FLAGS_RETAIN_HANDLING_TYPE_SHIFT = exports.SUBSCRIPTION_FLAGS_RETAIN_AS_PUBLISHED = exports.SUBSCRIPTION_FLAGS_NO_LOCAL = exports.PUBLISH_FLAGS_DUPLICATE = exports.PUBLISH_FLAGS_RETAIN = exports.PUBLISH_FLAGS_QOS_SHIFT = exports.CONNACK_FLAGS_SESSION_PRESENT = exports.CONNECT_FLAGS_CLEAN_SESSION = exports.CONNECT_FLAGS_WILL_RETAIN = exports.CONNECT_FLAGS_QOS_SHIFT = exports.CONNECT_FLAGS_HAS_WILL = exports.CONNECT_FLAGS_HAS_PASSWORD = exports.CONNECT_FLAGS_HAS_USERNAME = exports.CONNECT_5_PROTOCOL_DATAVIEW = exports.CONNECT_5_PROTOCOL_BUFFER = exports.CONNECT_5_PROTOCOL_BYTES = exports.CONNECT_311_PROTOCOL_DATAVIEW = exports.CONNECT_311_PROTOCOL_BUFFER = exports.CONNECT_311_PROTOCOL_BYTES = exports.SUBSCRIPTION_IDENTIFIER_PROPERTY_CODE = exports.TOPIC_ALIAS_PROPERTY_CODE = exports.AUTHENTICATION_DATA_PROPERTY_CODE = exports.AUTHENTICATION_METHOD_PROPERTY_CODE = exports.SERVER_REFERENCE_PROPERTY_CODE = exports.RESPONSE_INFORMATION_PROPERTY_CODE = exports.SERVER_KEEP_ALIVE_PROPERTY_CODE = exports.SHARED_SUBSCRIPTIONS_AVAILABLE_PROPERTY_CODE = exports.SUBSCRIPTION_IDENTIFIERS_AVAILABLE_PROPERTY_CODE = exports.WILDCARD_SUBSCRIPTIONS_AVAILABLE_PROPERTY_CODE = exports.REASON_STRING_PROPERTY_CODE = exports.ASSIGNED_CLIENT_IDENTIFIER_PROPERTY_CODE = exports.RETAIN_AVAILABLE_PROPERTY_CODE = exports.MAXIMUM_QOS_PROPERTY_CODE = exports.CORRELATION_DATA_PROPERTY_CODE = exports.RESPONSE_TOPIC_PROPERTY_CODE = exports.CONTENT_TYPE_PROPERTY_CODE = exports.MESSAGE_EXPIRY_INTERVAL_PROPERTY_CODE = exports.PAYLOAD_FORMAT_INDICATOR_PROPERTY_CODE = exports.WILL_DELAY_INTERVAL_PROPERTY_CODE = exports.REQUEST_PROBLEM_INFORMATION_PROPERTY_CODE = exports.REQUEST_RESPONSE_INFORMATION_PROPERTY_CODE = exports.TOPIC_ALIAS_MAXIMUM_PROPERTY_CODE = exports.MAXIMUM_PACKET_SIZE_PROPERTY_CODE = exports.RECEIVE_MAXIMUM_PROPERTY_CODE = exports.SESSION_EXPIRY_INTERVAL_PROPERTY_CODE = exports.USER_PROPERTY_PROPERTY_CODE = void 0;
exports.internalPacketToLogString = exports.disconnectPacketToLogString = exports.publishPacketToLogString = exports.connackPacketToLogString = exports.appendUserProperties = exports.cloneDisconnectShallow = exports.clonePublishShallow = exports.cloneUnsubscribeShallow = exports.cloneSubscribeShallow = exports.convertInternalPacketToBinary = exports.convertDisconnectPacketToBinary = exports.convertUnsubscribePacketToBinary = exports.convertSubscribePacketToBinary = exports.convertPublishPacketToBinary = exports.payloadToArrayBuffer = exports.isValidPayload = exports.convertUserPropertiesToBinary = exports.binaryDataToArrayBuffer = exports.isValidBinaryData = exports.RETAIN_HANDLING_TYPE_SHIFT = exports.QOS_MASK = exports.PACKET_TYPE_DISCONNECT_FULL_ENCODING_311 = exports.PACKET_TYPE_PINGRESP_FULL_ENCODING = exports.PACKET_TYPE_PINGREQ_FULL_ENCODING = exports.PACKET_TYPE_FIRST_BYTE_DISCONNECT = exports.PACKET_TYPE_FIRST_BYTE_UNSUBACK = exports.PACKET_TYPE_FIRST_BYTE_UNSUBSCRIBE = exports.PACKET_TYPE_FIRST_BYTE_SUBACK = exports.PACKET_TYPE_FIRST_BYTE_SUBSCRIBE = void 0;
var error_1 = require("../error");
var mqtt5_packet = __importStar(require("../../common/mqtt5_packet"));
var log = __importStar(require("../../common/log"));
// A variety of constants related to the MQTT311 and MQTT5 protocols
exports.USER_PROPERTY_PROPERTY_CODE = 0x26;
exports.SESSION_EXPIRY_INTERVAL_PROPERTY_CODE = 0x11;
exports.RECEIVE_MAXIMUM_PROPERTY_CODE = 0x21;
exports.MAXIMUM_PACKET_SIZE_PROPERTY_CODE = 0x27;
exports.TOPIC_ALIAS_MAXIMUM_PROPERTY_CODE = 0x22;
exports.REQUEST_RESPONSE_INFORMATION_PROPERTY_CODE = 0x19;
exports.REQUEST_PROBLEM_INFORMATION_PROPERTY_CODE = 0x17;
exports.WILL_DELAY_INTERVAL_PROPERTY_CODE = 0x18;
exports.PAYLOAD_FORMAT_INDICATOR_PROPERTY_CODE = 0x01;
exports.MESSAGE_EXPIRY_INTERVAL_PROPERTY_CODE = 0x02;
exports.CONTENT_TYPE_PROPERTY_CODE = 0x03;
exports.RESPONSE_TOPIC_PROPERTY_CODE = 0x08;
exports.CORRELATION_DATA_PROPERTY_CODE = 0x09;
exports.MAXIMUM_QOS_PROPERTY_CODE = 0x24;
exports.RETAIN_AVAILABLE_PROPERTY_CODE = 0x25;
exports.ASSIGNED_CLIENT_IDENTIFIER_PROPERTY_CODE = 0x12;
exports.REASON_STRING_PROPERTY_CODE = 0x1F;
exports.WILDCARD_SUBSCRIPTIONS_AVAILABLE_PROPERTY_CODE = 0x28;
exports.SUBSCRIPTION_IDENTIFIERS_AVAILABLE_PROPERTY_CODE = 0x29;
exports.SHARED_SUBSCRIPTIONS_AVAILABLE_PROPERTY_CODE = 0x2A;
exports.SERVER_KEEP_ALIVE_PROPERTY_CODE = 0x13;
exports.RESPONSE_INFORMATION_PROPERTY_CODE = 0x1A;
exports.SERVER_REFERENCE_PROPERTY_CODE = 0x1C;
exports.AUTHENTICATION_METHOD_PROPERTY_CODE = 0x15;
exports.AUTHENTICATION_DATA_PROPERTY_CODE = 0x16;
exports.TOPIC_ALIAS_PROPERTY_CODE = 0x23;
exports.SUBSCRIPTION_IDENTIFIER_PROPERTY_CODE = 0x0B;
exports.CONNECT_311_PROTOCOL_BYTES = [0x00, 0x04, 0x4D, 0x51, 0x54, 0x54, 0x04];
exports.CONNECT_311_PROTOCOL_BUFFER = new Uint8Array(exports.CONNECT_311_PROTOCOL_BYTES);
exports.CONNECT_311_PROTOCOL_DATAVIEW = new DataView(exports.CONNECT_311_PROTOCOL_BUFFER.buffer);
exports.CONNECT_5_PROTOCOL_BYTES = [0x00, 0x04, 0x4D, 0x51, 0x54, 0x54, 0x05];
exports.CONNECT_5_PROTOCOL_BUFFER = new Uint8Array(exports.CONNECT_5_PROTOCOL_BYTES);
exports.CONNECT_5_PROTOCOL_DATAVIEW = new DataView(exports.CONNECT_5_PROTOCOL_BUFFER.buffer);
exports.CONNECT_FLAGS_HAS_USERNAME = 0x80;
exports.CONNECT_FLAGS_HAS_PASSWORD = 0x40;
exports.CONNECT_FLAGS_HAS_WILL = 0x04;
exports.CONNECT_FLAGS_QOS_SHIFT = 0x03;
exports.CONNECT_FLAGS_WILL_RETAIN = 0x20;
exports.CONNECT_FLAGS_CLEAN_SESSION = 0x02;
exports.CONNACK_FLAGS_SESSION_PRESENT = 0x01;
exports.PUBLISH_FLAGS_QOS_SHIFT = 0x01;
exports.PUBLISH_FLAGS_RETAIN = 0x01;
exports.PUBLISH_FLAGS_DUPLICATE = 0x08;
exports.SUBSCRIPTION_FLAGS_NO_LOCAL = 0x04;
exports.SUBSCRIPTION_FLAGS_RETAIN_AS_PUBLISHED = 0x08;
exports.SUBSCRIPTION_FLAGS_RETAIN_HANDLING_TYPE_SHIFT = 0x04;
exports.PACKET_TYPE_FIRST_BYTE_CONNECT = 0x10;
exports.PACKET_TYPE_FIRST_BYTE_CONNACK = 0x20;
exports.PACKET_TYPE_FIRST_BYTE_PUBLISH = 0x30;
exports.PACKET_TYPE_FIRST_BYTE_PUBACK = 0x40;
exports.PACKET_TYPE_FIRST_BYTE_SUBSCRIBE = 0x82;
exports.PACKET_TYPE_FIRST_BYTE_SUBACK = 0x90;
exports.PACKET_TYPE_FIRST_BYTE_UNSUBSCRIBE = 0xA2;
exports.PACKET_TYPE_FIRST_BYTE_UNSUBACK = 0xB0;
exports.PACKET_TYPE_FIRST_BYTE_DISCONNECT = 0xE0;
exports.PACKET_TYPE_PINGREQ_FULL_ENCODING = 0xC000;
exports.PACKET_TYPE_PINGRESP_FULL_ENCODING = 0xD000;
exports.PACKET_TYPE_DISCONNECT_FULL_ENCODING_311 = 0xE000;
exports.QOS_MASK = 0x03;
exports.RETAIN_HANDLING_TYPE_SHIFT = 0x03;
function isValidBinaryData(data) {
    return data instanceof ArrayBuffer || data instanceof Uint8Array || data instanceof Buffer;
}
exports.isValidBinaryData = isValidBinaryData;
function binaryDataToArrayBuffer(data) {
    if (data instanceof ArrayBuffer) {
        return data;
    }
    else if (data instanceof Buffer) {
        return data.buffer.slice(data.byteOffset, data.byteOffset + data.byteLength);
    }
    else if (data instanceof Uint8Array) {
        return data.buffer;
    }
    else {
        throw new error_1.CrtError("Invalid binary data");
    }
}
exports.binaryDataToArrayBuffer = binaryDataToArrayBuffer;
function convertUserPropertiesToBinary(properties) {
    var e_1, _a;
    var encoder = new TextEncoder();
    var internal_properties = [];
    try {
        for (var properties_1 = __values(properties), properties_1_1 = properties_1.next(); !properties_1_1.done; properties_1_1 = properties_1.next()) {
            var property = properties_1_1.value;
            internal_properties.push({
                name: encoder.encode(property.name).buffer,
                value: encoder.encode(property.value).buffer
            });
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (properties_1_1 && !properties_1_1.done && (_a = properties_1.return)) _a.call(properties_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return internal_properties;
}
exports.convertUserPropertiesToBinary = convertUserPropertiesToBinary;
function convertConnectPacketToBinary(packet) {
    var encoder = new TextEncoder();
    var binary_packet = {
        type: mqtt5_packet.PacketType.Connect,
        cleanSession: packet.cleanStart ? 1 : 0,
        keepAliveIntervalSeconds: packet.keepAliveIntervalSeconds
    };
    if (packet.clientId != undefined) {
        binary_packet.clientId = encoder.encode(packet.clientId).buffer;
    }
    if (packet.username != undefined) {
        binary_packet.username = encoder.encode(packet.username).buffer;
    }
    if (packet.password != undefined) {
        binary_packet.password = binaryDataToArrayBuffer(packet.password);
    }
    if (packet.topicAliasMaximum != undefined) {
        binary_packet.topicAliasMaximum = packet.topicAliasMaximum;
    }
    if (packet.sessionExpiryIntervalSeconds != undefined) {
        binary_packet.sessionExpiryIntervalSeconds = packet.sessionExpiryIntervalSeconds;
    }
    if (packet.requestResponseInformation != undefined) {
        binary_packet.requestResponseInformation = packet.requestResponseInformation ? 1 : 0;
    }
    if (packet.requestProblemInformation != undefined) {
        binary_packet.requestProblemInformation = packet.requestProblemInformation ? 1 : 0;
    }
    if (packet.receiveMaximum != undefined) {
        binary_packet.receiveMaximum = packet.receiveMaximum;
    }
    if (packet.maximumPacketSizeBytes != undefined) {
        binary_packet.maximumPacketSizeBytes = packet.maximumPacketSizeBytes;
    }
    if (packet.willDelayIntervalSeconds != undefined) {
        binary_packet.willDelayIntervalSeconds = packet.willDelayIntervalSeconds;
    }
    if (packet.will) {
        binary_packet.will = convertPublishPacketToBinary(packet.will);
    }
    if (packet.authenticationMethod != undefined) {
        binary_packet.authenticationMethod = encoder.encode(packet.authenticationMethod).buffer;
    }
    if (packet.authenticationData != undefined) {
        binary_packet.authenticationData = binaryDataToArrayBuffer(packet.authenticationData);
    }
    if (packet.userProperties != undefined) {
        binary_packet.userProperties = convertUserPropertiesToBinary(packet.userProperties);
    }
    return binary_packet;
}
function convertConnectPacketInternalToBinary(packet, includeInternalFields) {
    var encoder = new TextEncoder();
    var binary_packet = convertConnectPacketToBinary(packet);
    if (includeInternalFields) {
        binary_packet.cleanSession = packet.cleanStart ? 1 : 0;
        if (packet.topicAliasMaximum != undefined) {
            binary_packet.topicAliasMaximum = packet.topicAliasMaximum;
        }
        if (packet.authenticationMethod != undefined) {
            binary_packet.authenticationMethod = encoder.encode(packet.authenticationMethod).buffer;
        }
        if (packet.authenticationData != undefined) {
            binary_packet.authenticationData = binaryDataToArrayBuffer(packet.authenticationData);
        }
    }
    return binary_packet;
}
function isValidPayload(payload) {
    return payload instanceof ArrayBuffer || payload instanceof Uint8Array || payload instanceof Buffer || typeof (payload) === 'string';
}
exports.isValidPayload = isValidPayload;
function payloadToArrayBuffer(payload) {
    if (payload instanceof ArrayBuffer) {
        return payload;
    }
    else if (payload instanceof Buffer) {
        return payload.buffer.slice(payload.byteOffset, payload.byteOffset + payload.byteLength);
    }
    else if (payload instanceof Uint8Array) {
        return payload.buffer;
    }
    else if (typeof (payload) === 'string') {
        var encoder = new TextEncoder();
        return encoder.encode(payload).buffer;
    }
    else {
        throw new error_1.CrtError("Invalid payload");
    }
}
exports.payloadToArrayBuffer = payloadToArrayBuffer;
function convertPublishPacketToBinary(packet) {
    var encoder = new TextEncoder();
    var binary_packet = {
        type: mqtt5_packet.PacketType.Publish,
        topicName: encoder.encode(packet.topicName).buffer,
        qos: packet.qos,
    };
    if (packet.payload != undefined) {
        binary_packet.payload = payloadToArrayBuffer(packet.payload);
    }
    if (packet.retain != undefined) {
        binary_packet.retain = packet.retain ? 1 : 0;
    }
    if (packet.payloadFormat != undefined) {
        binary_packet.payloadFormat = packet.payloadFormat;
    }
    if (packet.messageExpiryIntervalSeconds != undefined) {
        binary_packet.messageExpiryIntervalSeconds = packet.messageExpiryIntervalSeconds;
    }
    if (packet.topicAlias != undefined) {
        binary_packet.topicAlias = packet.topicAlias;
    }
    if (packet.responseTopic != undefined) {
        binary_packet.responseTopic = encoder.encode(packet.responseTopic).buffer;
    }
    if (packet.correlationData != undefined) {
        binary_packet.correlationData = binaryDataToArrayBuffer(packet.correlationData);
    }
    if (packet.contentType != undefined) {
        binary_packet.contentType = encoder.encode(packet.contentType).buffer;
    }
    if (packet.userProperties != undefined) {
        binary_packet.userProperties = convertUserPropertiesToBinary(packet.userProperties);
    }
    return binary_packet;
}
exports.convertPublishPacketToBinary = convertPublishPacketToBinary;
function convertPublishPacketInternalToBinary(packet, includeInternalFields) {
    var binary_packet = convertPublishPacketToBinary(packet);
    if (includeInternalFields) {
        if (packet.packetId != undefined) {
            binary_packet.packetId = packet.packetId;
        }
        if (packet.subscriptionIdentifiers != undefined) {
            binary_packet.subscriptionIdentifiers = packet.subscriptionIdentifiers;
        }
        binary_packet.duplicate = packet.duplicate ? 1 : 0;
    }
    return binary_packet;
}
function convertPubackPacketToBinary(packet) {
    var encoder = new TextEncoder();
    var binary_packet = {
        type: mqtt5_packet.PacketType.Puback,
        packetId: 0,
        reasonCode: packet.reasonCode
    };
    if (packet.reasonString != undefined) {
        binary_packet.reasonString = encoder.encode(packet.reasonString).buffer;
    }
    if (packet.userProperties != undefined) {
        binary_packet.userProperties = convertUserPropertiesToBinary(packet.userProperties);
    }
    return binary_packet;
}
function convertPubackPacketInternalToBinary(packet, includeInternalFields) {
    var binary_packet = convertPubackPacketToBinary(packet);
    if (includeInternalFields) {
        binary_packet.packetId = packet.packetId;
    }
    return binary_packet;
}
function convertSubscriptionToBinary(subscription, encoder) {
    var internal_subscription = {
        topicFilter: encoder.encode(subscription.topicFilter).buffer,
        topicFilterAsString: subscription.topicFilter,
        qos: subscription.qos
    };
    if (subscription.noLocal != undefined) {
        internal_subscription.noLocal = subscription.noLocal ? 1 : 0;
    }
    if (subscription.retainAsPublished != undefined) {
        internal_subscription.retainAsPublished = subscription.retainAsPublished ? 1 : 0;
    }
    if (subscription.retainHandlingType != undefined) {
        internal_subscription.retainHandlingType = subscription.retainHandlingType;
    }
    return internal_subscription;
}
function convertSubscribePacketToBinary(packet) {
    var e_2, _a;
    var encoder = new TextEncoder();
    var binary_packet = {
        type: mqtt5_packet.PacketType.Subscribe,
        packetId: 0,
        subscriptions: []
    };
    try {
        for (var _b = __values(packet.subscriptions), _c = _b.next(); !_c.done; _c = _b.next()) {
            var subscription = _c.value;
            binary_packet.subscriptions.push(convertSubscriptionToBinary(subscription, encoder));
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_2) throw e_2.error; }
    }
    if (packet.subscriptionIdentifier != undefined) {
        binary_packet.subscriptionIdentifier = packet.subscriptionIdentifier;
    }
    if (packet.userProperties != undefined) {
        binary_packet.userProperties = convertUserPropertiesToBinary(packet.userProperties);
    }
    return binary_packet;
}
exports.convertSubscribePacketToBinary = convertSubscribePacketToBinary;
function convertSubscribePacketInternalToBinary(packet, includeInternalFields) {
    var binary_packet = convertSubscribePacketToBinary(packet);
    if (includeInternalFields) {
        binary_packet.packetId = packet.packetId;
    }
    return binary_packet;
}
function convertUnsubscribePacketToBinary(packet) {
    var e_3, _a;
    var encoder = new TextEncoder();
    var binary_packet = {
        type: mqtt5_packet.PacketType.Unsubscribe,
        packetId: 0,
        topicFilters: [],
        topicFiltersAsStrings: []
    };
    try {
        for (var _b = __values(packet.topicFilters), _c = _b.next(); !_c.done; _c = _b.next()) {
            var topicFilter = _c.value;
            binary_packet.topicFilters.push(encoder.encode(topicFilter).buffer);
            binary_packet.topicFiltersAsStrings.push(topicFilter);
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_3) throw e_3.error; }
    }
    if (packet.userProperties != undefined) {
        binary_packet.userProperties = convertUserPropertiesToBinary(packet.userProperties);
    }
    return binary_packet;
}
exports.convertUnsubscribePacketToBinary = convertUnsubscribePacketToBinary;
function convertUnsubscribePacketInternalToBinary(packet, includeInternalFields) {
    var binaryPacket = convertUnsubscribePacketToBinary(packet);
    if (includeInternalFields) {
        binaryPacket.packetId = packet.packetId;
    }
    return binaryPacket;
}
function convertDisconnectPacketToBinary(packet) {
    var encoder = new TextEncoder();
    var binary_packet = {
        type: mqtt5_packet.PacketType.Disconnect,
        reasonCode: packet.reasonCode
    };
    if (packet.sessionExpiryIntervalSeconds != undefined) {
        binary_packet.sessionExpiryIntervalSeconds = packet.sessionExpiryIntervalSeconds;
    }
    if (packet.serverReference != undefined) {
        binary_packet.serverReference = encoder.encode(packet.serverReference).buffer;
    }
    if (packet.reasonString != undefined) {
        binary_packet.reasonString = encoder.encode(packet.reasonString).buffer;
    }
    if (packet.userProperties != undefined) {
        binary_packet.userProperties = convertUserPropertiesToBinary(packet.userProperties);
    }
    return binary_packet;
}
exports.convertDisconnectPacketToBinary = convertDisconnectPacketToBinary;
/*
 * Mostly needed for round trip encode/decode testing.  In the client implementation, we use
 * convert_user_packet_to_binary on user-submitted packets after initial validation.
 */
function convertInternalPacketToBinary(packet) {
    if (packet.type == undefined) {
        throw new error_1.CrtError("Invalid packet type");
    }
    switch (packet.type) {
        case mqtt5_packet.PacketType.Connect:
            return convertConnectPacketInternalToBinary(packet, true);
        case mqtt5_packet.PacketType.Publish:
            return convertPublishPacketInternalToBinary(packet, true);
        case mqtt5_packet.PacketType.Puback:
            return convertPubackPacketInternalToBinary(packet, true);
        case mqtt5_packet.PacketType.Subscribe:
            return convertSubscribePacketInternalToBinary(packet, true);
        case mqtt5_packet.PacketType.Unsubscribe:
            return convertUnsubscribePacketInternalToBinary(packet, true);
        case mqtt5_packet.PacketType.Disconnect:
            return convertDisconnectPacketToBinary(packet);
        case mqtt5_packet.PacketType.Pingreq:
            return {
                type: mqtt5_packet.PacketType.Pingreq
            };
        default:
            throw new error_1.CrtError("Unexpected/unsupported internal packet type: ".concat(packet.type));
    }
}
exports.convertInternalPacketToBinary = convertInternalPacketToBinary;
function cloneSubscribeShallow(subscribe) {
    var clone = {
        type: mqtt5_packet.PacketType.Subscribe,
        subscriptions: subscribe.subscriptions
    };
    if (subscribe.subscriptionIdentifier) {
        clone.subscriptionIdentifier = subscribe.subscriptionIdentifier;
    }
    if (subscribe.userProperties) {
        clone.userProperties = subscribe.userProperties;
    }
    return clone;
}
exports.cloneSubscribeShallow = cloneSubscribeShallow;
function cloneUnsubscribeShallow(unsubscribe) {
    var clone = {
        type: mqtt5_packet.PacketType.Unsubscribe,
        topicFilters: unsubscribe.topicFilters
    };
    if (unsubscribe.userProperties) {
        clone.userProperties = unsubscribe.userProperties;
    }
    return clone;
}
exports.cloneUnsubscribeShallow = cloneUnsubscribeShallow;
function clonePublishShallow(publish) {
    var clone = {
        type: mqtt5_packet.PacketType.Publish,
        topicName: publish.topicName,
        qos: publish.qos
    };
    if (publish.payload) {
        clone.payload = publish.payload;
    }
    if (publish.retain) {
        clone.retain = publish.retain;
    }
    if (publish.payloadFormat) {
        clone.payloadFormat = publish.payloadFormat;
    }
    if (publish.messageExpiryIntervalSeconds) {
        clone.messageExpiryIntervalSeconds = publish.messageExpiryIntervalSeconds;
    }
    if (publish.responseTopic) {
        clone.responseTopic = publish.responseTopic;
    }
    if (publish.correlationData) {
        clone.correlationData = publish.correlationData;
    }
    if (publish.contentType) {
        clone.contentType = publish.contentType;
    }
    if (publish.userProperties) {
        clone.userProperties = publish.userProperties;
    }
    return clone;
}
exports.clonePublishShallow = clonePublishShallow;
function cloneDisconnectShallow(disconnect) {
    var clone = {
        type: mqtt5_packet.PacketType.Disconnect,
        reasonCode: disconnect.reasonCode
    };
    if (disconnect.sessionExpiryIntervalSeconds) {
        clone.sessionExpiryIntervalSeconds = disconnect.sessionExpiryIntervalSeconds;
    }
    if (disconnect.reasonString) {
        clone.reasonString = disconnect.reasonString;
    }
    if (disconnect.userProperties) {
        clone.userProperties = disconnect.userProperties;
    }
    if (disconnect.serverReference) {
        clone.serverReference = disconnect.serverReference;
    }
    return clone;
}
exports.cloneDisconnectShallow = cloneDisconnectShallow;
function appendPayloadPropertyLine(current, prefix, propertyName, value) {
    var payloadLength = 0;
    if (value != undefined) {
        if (value instanceof ArrayBuffer) {
            payloadLength = value.byteLength;
        }
        else if (value instanceof Buffer) {
            payloadLength = value.byteLength;
        }
        else if (value instanceof Uint8Array) {
            payloadLength = value.buffer.byteLength;
        }
        else if (typeof (value) === 'string') {
            var encoder = new TextEncoder();
            payloadLength = encoder.encode(value).buffer.byteLength;
        }
    }
    return current + "".concat(prefix, "  ").concat(propertyName, ": [..").concat(payloadLength, " bytes..]\n");
}
function appendUserProperties(current, prefix, userProperties) {
    var e_4, _a;
    if (!userProperties) {
        return current;
    }
    current += "".concat(prefix, "  UserProperties: [\n");
    try {
        for (var userProperties_1 = __values(userProperties), userProperties_1_1 = userProperties_1.next(); !userProperties_1_1.done; userProperties_1_1 = userProperties_1.next()) {
            var userProperty = userProperties_1_1.value;
            current += "".concat(prefix, "    \"").concat(userProperty.name, "\" : \"").concat(userProperty.value, "\"\n");
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (userProperties_1_1 && !userProperties_1_1.done && (_a = userProperties_1.return)) _a.call(userProperties_1);
        }
        finally { if (e_4) throw e_4.error; }
    }
    current += "".concat(prefix, "  ]\n");
    return current;
}
exports.appendUserProperties = appendUserProperties;
function connectPacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Connect: {\n");
    result = log.appendBooleanPropertyLine(result, prefix, "CleanStart", packet.cleanStart);
    result = log.appendNumericPropertyLine(result, prefix, "KeepAliveInterval", packet.keepAliveIntervalSeconds);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ClientId", packet.clientId);
    // keep username opaque intentionally; there can be authentication-sensitive data in it
    result = log.appendOptionalBytesPropertyLine(result, prefix, "Username", packet.username);
    result = log.appendOptionalBytesPropertyLine(result, prefix, "Password", packet.password);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "SessionExpiryIntervalSeconds", packet.sessionExpiryIntervalSeconds);
    result = log.appendOptionalBooleanPropertyLine(result, prefix, "RequestResponseInformation", packet.requestResponseInformation);
    result = log.appendOptionalBooleanPropertyLine(result, prefix, "RequestProblemInformation", packet.requestProblemInformation);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "ReceiveMaximum", packet.receiveMaximum);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "MaximumPacketSize", packet.maximumPacketSizeBytes);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "TopicAliasMaximum", packet.topicAliasMaximum);
    result = log.appendOptionalStringPropertyLine(result, prefix, "AuthenticationMethod", packet.authenticationMethod);
    result = log.appendOptionalBytesPropertyLine(result, prefix, "AuthenticationData", packet.authenticationData);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "WillDelayIntervalSeconds", packet.willDelayIntervalSeconds);
    if (packet.will) {
        result += "".concat(prefix, "  Will: {\n");
        result += publishPacketToLogString(packet.will, prefix + "  ");
        result += "".concat(prefix, "  }\n");
    }
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
function connackPacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Connack: {\n");
    result = log.appendBooleanPropertyLine(result, prefix, "SessionPresent", packet.sessionPresent);
    result = log.appendEnumPropertyLine(result, prefix, "ReasonCode", function (val) { return mqtt5_packet.ConnectReasonCode[val]; }, packet.reasonCode);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "SessionExpiryInterval", packet.sessionExpiryInterval);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "ReceiveMaximum", packet.receiveMaximum);
    result = log.appendOptionalEnumPropertyLine(result, prefix, "MaximumQos", function (val) { return mqtt5_packet.QoS[val]; }, packet.maximumQos);
    result = log.appendOptionalBooleanPropertyLine(result, prefix, "RetainAvailable", packet.retainAvailable);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "MaximumPacketSize", packet.maximumPacketSize);
    result = log.appendOptionalStringPropertyLine(result, prefix, "AssignedClientIdentifier", packet.assignedClientIdentifier);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "TopicAliasMaximum", packet.topicAliasMaximum);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ReasonString", packet.reasonString);
    result = log.appendOptionalBooleanPropertyLine(result, prefix, "WildcardSubscriptionsAvailable", packet.wildcardSubscriptionsAvailable);
    result = log.appendOptionalBooleanPropertyLine(result, prefix, "SubscriptionIdentifiersAvailable", packet.subscriptionIdentifiersAvailable);
    result = log.appendOptionalBooleanPropertyLine(result, prefix, "SharedSubscriptionsAvailable", packet.sharedSubscriptionsAvailable);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "ServerKeepAlive", packet.serverKeepAlive);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ResponseInformation", packet.responseInformation);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ServerReference", packet.serverReference);
    result = log.appendOptionalStringPropertyLine(result, prefix, "AuthenticationMethod", packet.authenticationMethod);
    result = log.appendOptionalBytesPropertyLine(result, prefix, "AuthenticationData", packet.authenticationData);
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
exports.connackPacketToLogString = connackPacketToLogString;
function publishPacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Publish: {\n");
    result = log.appendOptionalNumericPropertyLine(result, prefix, "PacketId", packet.packetId);
    result = log.appendBooleanPropertyLine(result, prefix, "Duplicate", packet.duplicate);
    result = log.appendStringPropertyLine(result, prefix, "TopicName", packet.topicName);
    result = appendPayloadPropertyLine(result, prefix, "Payload", packet.payload);
    result = log.appendEnumPropertyLine(result, prefix, "Qos", function (val) { return mqtt5_packet.QoS[val]; }, packet.qos);
    result = log.appendOptionalBooleanPropertyLine(result, prefix, "Retain", packet.retain);
    result = log.appendOptionalEnumPropertyLine(result, prefix, "PayloadFormat", function (val) { return mqtt5_packet.PayloadFormatIndicator[val]; }, packet.payloadFormat);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "MessageExpiryIntervalSeconds", packet.messageExpiryIntervalSeconds);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "TopicAlias", packet.topicAlias);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ResponseTopic", packet.responseTopic);
    result = log.appendOptionalBytesPropertyLine(result, prefix, "CorrelationData", packet.correlationData);
    result = log.appendOptionalNumericArrayPropertyLine(result, prefix, "SubscriptionIdentifiers", packet.subscriptionIdentifiers);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ContentType", packet.contentType);
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
exports.publishPacketToLogString = publishPacketToLogString;
function pubackPacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Puback: {\n");
    result = log.appendNumericPropertyLine(result, prefix, "PacketId", packet.packetId);
    result = log.appendEnumPropertyLine(result, prefix, "ReasonCode", function (val) { return mqtt5_packet.PubackReasonCode[val]; }, packet.reasonCode);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ReasonString", packet.reasonString);
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
function appendSubscription(current, prefix, subscription) {
    var localPrefix = "".concat(prefix, "  ");
    var result = "".concat(localPrefix, "{");
    result = log.appendStringPropertyLine(result, localPrefix, "TopicFilter", subscription.topicFilter);
    result = log.appendEnumPropertyLine(result, localPrefix, "Qos", function (val) { return mqtt5_packet.QoS[val]; }, subscription.qos);
    result = log.appendOptionalBooleanPropertyLine(result, localPrefix, "NoLocal", subscription.noLocal);
    result = log.appendOptionalBooleanPropertyLine(result, localPrefix, "RetainAsPublished", subscription.retainAsPublished);
    result = log.appendOptionalEnumPropertyLine(result, localPrefix, "RetainHandlingType", function (val) { return mqtt5_packet.RetainHandlingType[val]; }, subscription.retainHandlingType);
    result += "".concat(localPrefix, "}");
    return result;
}
function subscribePacketToLogString(packet, prefix) {
    var e_5, _a;
    var result = "".concat(prefix, "Subscribe: {\n");
    result = log.appendOptionalNumericPropertyLine(result, prefix, "PacketId", packet.packetId);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "SubscriptionIdentifier", packet.subscriptionIdentifier);
    result += "".concat(prefix, "  Subscriptions: [\n");
    try {
        for (var _b = __values(packet.subscriptions), _c = _b.next(); !_c.done; _c = _b.next()) {
            var subscription = _c.value;
            result += appendSubscription(result, prefix + "  ", subscription);
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_5) throw e_5.error; }
    }
    result += "".concat(prefix, "  ]\n");
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
function subackPacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Suback: {\n");
    result = log.appendNumericPropertyLine(result, prefix, "PacketId", packet.packetId);
    result = log.appendEnumArrayPropertyLine(result, prefix, "ReasonCodes", function (val) { return mqtt5_packet.SubackReasonCode[val]; }, packet.reasonCodes);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ReasonString", packet.reasonString);
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
function unsubscribePacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Unsubscribe: {\n");
    result = log.appendOptionalNumericPropertyLine(result, prefix, "PacketId", packet.packetId);
    result = log.appendStringArrayPropertyLine(result, prefix, "TopicFilters", packet.topicFilters);
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
function unsubackPacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Unsuback: {\n");
    result = log.appendNumericPropertyLine(result, prefix, "PacketId", packet.packetId);
    result = log.appendEnumArrayPropertyLine(result, prefix, "ReasonCodes", function (val) { return mqtt5_packet.UnsubackReasonCode[val]; }, packet.reasonCodes);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ReasonString", packet.reasonString);
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
function disconnectPacketToLogString(packet, prefix) {
    var result = "".concat(prefix, "Disconnect: {\n");
    result = log.appendEnumPropertyLine(result, prefix, "ReasonCode", function (val) { return mqtt5_packet.DisconnectReasonCode[val]; }, packet.reasonCode);
    result = log.appendOptionalNumericPropertyLine(result, prefix, "SessionExpiryIntervalSeconds", packet.sessionExpiryIntervalSeconds);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ReasonString", packet.reasonString);
    result = log.appendOptionalStringPropertyLine(result, prefix, "ServerReference", packet.serverReference);
    result = appendUserProperties(result, prefix, packet.userProperties);
    result += "".concat(prefix, "}\n");
    return result;
}
exports.disconnectPacketToLogString = disconnectPacketToLogString;
// support all user-submitted packets + all valid inbound packets + Connect
function internalPacketToLogString(packet, prefix) {
    if (prefix === void 0) { prefix = ""; }
    switch (packet.type) {
        case mqtt5_packet.PacketType.Connect:
            return connectPacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Connack:
            return connackPacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Publish:
            return publishPacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Puback:
            return pubackPacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Subscribe:
            return subscribePacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Suback:
            return subackPacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Unsubscribe:
            return unsubscribePacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Unsuback:
            return unsubackPacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Disconnect:
            return disconnectPacketToLogString(packet, prefix);
        case mqtt5_packet.PacketType.Pingresp:
            return "".concat(prefix, "Pingresp: {}");
        default:
            return "".concat(prefix, "UnexpectedPacketType: ").concat(packet.type);
    }
}
exports.internalPacketToLogString = internalPacketToLogString;
//# sourceMappingURL=model.js.map