"use strict";
/*
 * Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.decodeVli = exports.VliDecodeResultType = exports.encodeVli = exports.getVliByteLength = void 0;
var error_1 = require("../error");
// assumes value is integral and non-negative
function getVliByteLength(value) {
    if (value < 128) {
        return 1;
    }
    else if (value < 16384) {
        return 2;
    }
    else if (value < 2097152) {
        return 3;
    }
    else if (value < 268435456) {
        return 4;
    }
    else {
        throw new error_1.CrtError("Invalid VLI value");
    }
}
exports.getVliByteLength = getVliByteLength;
// assumes value is integral and non-negative
function encodeVli(dest, value) {
    var i = 0;
    var hasMore = true;
    while (hasMore) {
        var byte = value & 0x7F;
        value = value >>> 7;
        hasMore = value > 0;
        if (hasMore) {
            byte = byte | 0x80;
            if (i >= 3) {
                throw new error_1.CrtError("Invalid VLI value");
            }
        }
        dest.setUint8(i++, byte);
    }
    return new DataView(dest.buffer, dest.byteOffset + i, dest.byteLength - i);
}
exports.encodeVli = encodeVli;
var VliDecodeResultType;
(function (VliDecodeResultType) {
    VliDecodeResultType[VliDecodeResultType["Success"] = 0] = "Success";
    VliDecodeResultType[VliDecodeResultType["MoreData"] = 1] = "MoreData";
})(VliDecodeResultType = exports.VliDecodeResultType || (exports.VliDecodeResultType = {}));
function decodeVli(data, offset) {
    if (offset < 0) {
        throw new error_1.CrtError("Invalid DataView offset");
    }
    if (offset >= data.byteLength) {
        return {
            type: VliDecodeResultType.MoreData
        };
    }
    var value = 0;
    var index = 0;
    var shift = 0;
    while (index < 4) {
        var view_index = offset + index++;
        var raw_byte = data.getUint8(view_index);
        var masked_byte = raw_byte & 0x7F;
        value += (masked_byte << shift);
        if (masked_byte == raw_byte) {
            return {
                type: VliDecodeResultType.Success,
                value: value,
                nextOffset: offset + index
            };
        }
        else if (view_index + 1 >= data.byteLength) {
            return {
                type: VliDecodeResultType.MoreData
            };
        }
        shift += 7;
    }
    throw new error_1.CrtError("Decoding failure - invalid VLI integer");
}
exports.decodeVli = decodeVli;
//# sourceMappingURL=vli.js.map