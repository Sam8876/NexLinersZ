"use strict";
/*
 * Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Decoder = exports.buildClientDecodingFunctionSet = exports.decodeUserProperty = exports.decodeLengthPrefixedBytes = exports.decodeBytes = exports.decodeLengthPrefixedString = exports.decodeVli = exports.decodeU32 = exports.decodeU16 = exports.decodeU8 = exports.decodeBoolean = void 0;
var error_1 = require("../error");
var mqtt5_packet = __importStar(require("../../common/mqtt5_packet"));
var mqtt_shared = __importStar(require("../../common/mqtt_shared"));
var vli = __importStar(require("./vli"));
var model = __importStar(require("./model"));
var util_utf8_browser_1 = require("@aws-sdk/util-utf8-browser");
// utility functions for individual packet fields
function decodeBoolean(payload, offset) {
    var value = payload.getUint8(offset);
    if (value == 0 || value == 1) {
        return [value == 1, offset + 1];
    }
    throw new error_1.CrtError("Boolean field encoded with invalid byte value");
}
exports.decodeBoolean = decodeBoolean;
function decodeU8(payload, offset) {
    return [payload.getUint8(offset), offset + 1];
}
exports.decodeU8 = decodeU8;
function decodeU16(payload, offset) {
    return [payload.getUint16(offset), offset + 2];
}
exports.decodeU16 = decodeU16;
function decodeU32(payload, offset) {
    return [payload.getUint32(offset), offset + 4];
}
exports.decodeU32 = decodeU32;
function decodeVli(payload, offset) {
    var result = vli.decodeVli(payload, offset);
    if (result.type == vli.VliDecodeResultType.Success) {
        // @ts-ignore
        return [result.value, result.nextOffset];
    }
    throw new error_1.CrtError("insufficient data to decode variable-length integer");
}
exports.decodeVli = decodeVli;
function decodeLengthPrefixedString(payload, offset) {
    var _a = __read(decodeU16(payload, offset), 2), stringLength = _a[0], index = _a[1];
    return [(0, util_utf8_browser_1.toUtf8)(new Uint8Array(payload.buffer, index, stringLength)), index + stringLength];
}
exports.decodeLengthPrefixedString = decodeLengthPrefixedString;
function decodeBytes(payload, offset, length) {
    return [Buffer.from(payload.buffer, offset, length), offset + length];
}
exports.decodeBytes = decodeBytes;
function decodeLengthPrefixedBytes(payload, offset) {
    var _a = __read(decodeU16(payload, offset), 2), bytesLength = _a[0], index = _a[1];
    return [Buffer.from(payload.buffer, index, bytesLength), index + bytesLength];
}
exports.decodeLengthPrefixedBytes = decodeLengthPrefixedBytes;
function decodeUserProperty(payload, offset, userProperties) {
    var _a, _b;
    var index = offset;
    var name = "";
    _a = __read(decodeLengthPrefixedString(payload, index), 2), name = _a[0], index = _a[1];
    var value = "";
    _b = __read(decodeLengthPrefixedString(payload, index), 2), value = _b[0], index = _b[1];
    userProperties.push({ name: name, value: value });
    return index;
}
exports.decodeUserProperty = decodeUserProperty;
// MQTT 311 Packet decoding functions
function decodeConnackPacket311(firstByte, payload) {
    var _a, _b;
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_CONNACK) {
        throw new error_1.CrtError("Connack with invalid first byte: " + firstByte);
    }
    if (payload.byteLength != 2) {
        throw new error_1.CrtError("Connack packet invalid payload length");
    }
    var index = 0;
    var flags = 0;
    var connack = {
        type: mqtt5_packet.PacketType.Connack,
        sessionPresent: false,
        reasonCode: mqtt5_packet.ConnectReasonCode.Success
    };
    _a = __read(decodeU8(payload, index), 2), flags = _a[0], index = _a[1];
    if ((flags & (~0x01)) != 0) {
        throw new error_1.CrtError("Connack invalid flags");
    }
    connack.sessionPresent = (flags & model.CONNACK_FLAGS_SESSION_PRESENT) != 0;
    _b = __read(decodeU8(payload, index), 2), connack.reasonCode = _b[0], index = _b[1];
    return connack;
}
function decodePublishPacket311(firstByte, payload) {
    var _a, _b, _c;
    var index = 0;
    var publish = {
        type: mqtt5_packet.PacketType.Publish,
        qos: (firstByte >>> model.PUBLISH_FLAGS_QOS_SHIFT) & model.QOS_MASK,
        duplicate: (firstByte & model.PUBLISH_FLAGS_DUPLICATE) ? true : false,
        retain: (firstByte & model.PUBLISH_FLAGS_RETAIN) ? true : false,
        topicName: ""
    };
    _a = __read(decodeLengthPrefixedString(payload, index), 2), publish.topicName = _a[0], index = _a[1];
    if (publish.qos > 0) {
        _b = __read(decodeU16(payload, index), 2), publish.packetId = _b[0], index = _b[1];
    }
    if (index < payload.byteLength) {
        _c = __read(decodeBytes(payload, index, payload.byteLength - index), 2), publish.payload = _c[0], index = _c[1];
    }
    return publish;
}
function decodePubackPacket311(firstByte, payload) {
    var _a;
    if (payload.byteLength != 2) {
        throw new error_1.CrtError("Puback packet with invalid payload length");
    }
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_PUBACK) {
        throw new error_1.CrtError("Puback packet with invalid first byte: " + firstByte);
    }
    var index = 0;
    var puback = {
        type: mqtt5_packet.PacketType.Puback,
        packetId: 0,
        reasonCode: mqtt5_packet.PubackReasonCode.Success,
    };
    _a = __read(decodeU16(payload, index), 2), puback.packetId = _a[0], index = _a[1];
    return puback;
}
function decodeSubackPacket311(firstByte, payload) {
    var _a, _b;
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_SUBACK) {
        throw new error_1.CrtError("Suback packet with invalid first byte: " + firstByte);
    }
    var index = 0;
    var suback = {
        type: mqtt5_packet.PacketType.Suback,
        packetId: 0,
        reasonCodes: new Array()
    };
    _a = __read(decodeU16(payload, index), 2), suback.packetId = _a[0], index = _a[1];
    var reasonCodeCount = payload.byteLength - index;
    for (var i = 0; i < reasonCodeCount; i++) {
        var reasonCode = 0;
        _b = __read(decodeU8(payload, index), 2), reasonCode = _b[0], index = _b[1];
        suback.reasonCodes.push(reasonCode);
    }
    return suback;
}
function decodeUnsubackPacket311(firstByte, payload) {
    var _a;
    if (payload.byteLength != 2) {
        throw new error_1.CrtError("Unsuback packet with invalid payload length");
    }
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_UNSUBACK) {
        throw new error_1.CrtError("Unsuback packet with invalid first byte: " + firstByte);
    }
    var index = 0;
    var puback = {
        type: mqtt5_packet.PacketType.Unsuback,
        packetId: 0,
        reasonCodes: [] // client will need to synthesize N successes based on original unsubscribe
    };
    _a = __read(decodeU16(payload, index), 2), puback.packetId = _a[0], index = _a[1];
    return puback;
}
function decodePingrespPacket(firstByte, payload) {
    if (payload.byteLength != 0) {
        throw new error_1.CrtError("Pingresp packet with invalid payload length");
    }
    if (firstByte != (model.PACKET_TYPE_PINGRESP_FULL_ENCODING >>> 8)) {
        throw new error_1.CrtError("Pingresp packet with invalid first byte: " + firstByte);
    }
    return {
        type: mqtt5_packet.PacketType.Pingresp
    };
}
// MQTT 5 packet decoders
function decodeConnackProperties(connack, payload, offset, propertyLength) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s;
    var index = offset;
    var propertyCode = 0;
    while (index < offset + propertyLength) {
        _a = __read(decodeU8(payload, index), 2), propertyCode = _a[0], index = _a[1];
        switch (propertyCode) {
            case model.SESSION_EXPIRY_INTERVAL_PROPERTY_CODE:
                _b = __read(decodeU32(payload, index), 2), connack.sessionExpiryInterval = _b[0], index = _b[1];
                break;
            case model.RECEIVE_MAXIMUM_PROPERTY_CODE:
                _c = __read(decodeU16(payload, index), 2), connack.receiveMaximum = _c[0], index = _c[1];
                break;
            case model.MAXIMUM_QOS_PROPERTY_CODE:
                _d = __read(decodeU8(payload, index), 2), connack.maximumQos = _d[0], index = _d[1];
                break;
            case model.RETAIN_AVAILABLE_PROPERTY_CODE:
                _e = __read(decodeBoolean(payload, index), 2), connack.retainAvailable = _e[0], index = _e[1];
                break;
            case model.MAXIMUM_PACKET_SIZE_PROPERTY_CODE:
                _f = __read(decodeU32(payload, index), 2), connack.maximumPacketSize = _f[0], index = _f[1];
                break;
            case model.ASSIGNED_CLIENT_IDENTIFIER_PROPERTY_CODE:
                _g = __read(decodeLengthPrefixedString(payload, index), 2), connack.assignedClientIdentifier = _g[0], index = _g[1];
                break;
            case model.TOPIC_ALIAS_MAXIMUM_PROPERTY_CODE:
                _h = __read(decodeU16(payload, index), 2), connack.topicAliasMaximum = _h[0], index = _h[1];
                break;
            case model.REASON_STRING_PROPERTY_CODE:
                _j = __read(decodeLengthPrefixedString(payload, index), 2), connack.reasonString = _j[0], index = _j[1];
                break;
            case model.USER_PROPERTY_PROPERTY_CODE:
                if (!connack.userProperties) {
                    connack.userProperties = new Array();
                }
                index = decodeUserProperty(payload, index, connack.userProperties);
                break;
            case model.WILDCARD_SUBSCRIPTIONS_AVAILABLE_PROPERTY_CODE:
                _k = __read(decodeBoolean(payload, index), 2), connack.wildcardSubscriptionsAvailable = _k[0], index = _k[1];
                break;
            case model.SUBSCRIPTION_IDENTIFIERS_AVAILABLE_PROPERTY_CODE:
                _l = __read(decodeBoolean(payload, index), 2), connack.subscriptionIdentifiersAvailable = _l[0], index = _l[1];
                break;
            case model.SHARED_SUBSCRIPTIONS_AVAILABLE_PROPERTY_CODE:
                _m = __read(decodeBoolean(payload, index), 2), connack.sharedSubscriptionsAvailable = _m[0], index = _m[1];
                break;
            case model.SERVER_KEEP_ALIVE_PROPERTY_CODE:
                _o = __read(decodeU16(payload, index), 2), connack.serverKeepAlive = _o[0], index = _o[1];
                break;
            case model.RESPONSE_INFORMATION_PROPERTY_CODE:
                _p = __read(decodeLengthPrefixedString(payload, index), 2), connack.responseInformation = _p[0], index = _p[1];
                break;
            case model.SERVER_REFERENCE_PROPERTY_CODE:
                _q = __read(decodeLengthPrefixedString(payload, index), 2), connack.serverReference = _q[0], index = _q[1];
                break;
            case model.AUTHENTICATION_METHOD_PROPERTY_CODE:
                _r = __read(decodeLengthPrefixedString(payload, index), 2), connack.authenticationMethod = _r[0], index = _r[1];
                break;
            case model.AUTHENTICATION_DATA_PROPERTY_CODE:
                _s = __read(decodeLengthPrefixedBytes(payload, index), 2), connack.authenticationData = _s[0], index = _s[1];
                break;
            default:
                throw new error_1.CrtError("Unknown Connack property code: " + propertyCode);
        }
    }
    if (index != offset + propertyLength) {
        throw new error_1.CrtError("Connack packet mismatch between encoded properties and expected length");
    }
    return index;
}
function decodeConnackPacket5(firstByte, payload) {
    var _a, _b, _c;
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_CONNACK) {
        throw new error_1.CrtError("Connack with invalid first byte: " + firstByte);
    }
    var index = 0;
    var flags = 0;
    var connack = {
        type: mqtt5_packet.PacketType.Connack,
        sessionPresent: false,
        reasonCode: mqtt5_packet.ConnectReasonCode.Success
    };
    _a = __read(decodeU8(payload, index), 2), flags = _a[0], index = _a[1];
    if ((flags & (~0x01)) != 0) {
        throw new error_1.CrtError("Connack invalid flags");
    }
    connack.sessionPresent = (flags & model.CONNACK_FLAGS_SESSION_PRESENT) != 0;
    _b = __read(decodeU8(payload, index), 2), connack.reasonCode = _b[0], index = _b[1];
    var propertiesLength = 0;
    _c = __read(decodeVli(payload, index), 2), propertiesLength = _c[0], index = _c[1];
    index = decodeConnackProperties(connack, payload, index, propertiesLength);
    if (index != payload.byteLength) {
        throw new error_1.CrtError("Connect packet mismatch between payload and expected length");
    }
    return connack;
}
function decodePublishProperties(publish, payload, offset, propertyLength) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    var index = offset;
    var propertyCode = 0;
    while (index < offset + propertyLength) {
        _a = __read(decodeU8(payload, index), 2), propertyCode = _a[0], index = _a[1];
        switch (propertyCode) {
            case model.PAYLOAD_FORMAT_INDICATOR_PROPERTY_CODE:
                _b = __read(decodeU8(payload, index), 2), publish.payloadFormat = _b[0], index = _b[1];
                break;
            case model.MESSAGE_EXPIRY_INTERVAL_PROPERTY_CODE:
                _c = __read(decodeU32(payload, index), 2), publish.messageExpiryIntervalSeconds = _c[0], index = _c[1];
                break;
            case model.TOPIC_ALIAS_PROPERTY_CODE:
                _d = __read(decodeU16(payload, index), 2), publish.topicAlias = _d[0], index = _d[1];
                break;
            case model.RESPONSE_TOPIC_PROPERTY_CODE:
                _e = __read(decodeLengthPrefixedString(payload, index), 2), publish.responseTopic = _e[0], index = _e[1];
                break;
            case model.CORRELATION_DATA_PROPERTY_CODE:
                _f = __read(decodeLengthPrefixedBytes(payload, index), 2), publish.correlationData = _f[0], index = _f[1];
                break;
            case model.USER_PROPERTY_PROPERTY_CODE:
                if (!publish.userProperties) {
                    publish.userProperties = new Array();
                }
                index = decodeUserProperty(payload, index, publish.userProperties);
                break;
            case model.SUBSCRIPTION_IDENTIFIER_PROPERTY_CODE:
                if (!publish.subscriptionIdentifiers) {
                    publish.subscriptionIdentifiers = new Array();
                }
                var subscriptionIdentifier = 0;
                _g = __read(decodeVli(payload, index), 2), subscriptionIdentifier = _g[0], index = _g[1];
                publish.subscriptionIdentifiers.push(subscriptionIdentifier);
                break;
            case model.CONTENT_TYPE_PROPERTY_CODE:
                _h = __read(decodeLengthPrefixedString(payload, index), 2), publish.contentType = _h[0], index = _h[1];
                break;
            default:
                throw new error_1.CrtError("Unknown Publish property code: " + propertyCode);
        }
    }
    if (index != offset + propertyLength) {
        throw new error_1.CrtError("Publish packet mismatch between encoded properties and expected length");
    }
    return index;
}
function decodePublishPacket5(firstByte, payload) {
    var _a, _b, _c, _d;
    var index = 0;
    var publish = {
        type: mqtt5_packet.PacketType.Publish,
        qos: (firstByte >>> model.PUBLISH_FLAGS_QOS_SHIFT) & model.QOS_MASK,
        duplicate: (firstByte & model.PUBLISH_FLAGS_DUPLICATE) ? true : false,
        retain: (firstByte & model.PUBLISH_FLAGS_RETAIN) ? true : false,
        topicName: ""
    };
    _a = __read(decodeLengthPrefixedString(payload, index), 2), publish.topicName = _a[0], index = _a[1];
    if (publish.qos > 0) {
        _b = __read(decodeU16(payload, index), 2), publish.packetId = _b[0], index = _b[1];
    }
    var propertiesLength = 0;
    _c = __read(decodeVli(payload, index), 2), propertiesLength = _c[0], index = _c[1];
    index = decodePublishProperties(publish, payload, index, propertiesLength);
    if (index < payload.byteLength) {
        _d = __read(decodeBytes(payload, index, payload.byteLength - index), 2), publish.payload = _d[0], index = _d[1];
    }
    return publish;
}
function decodePubackProperties(puback, payload, offset, propertyLength) {
    var _a, _b;
    var index = offset;
    var propertyCode = 0;
    while (index < offset + propertyLength) {
        _a = __read(decodeU8(payload, index), 2), propertyCode = _a[0], index = _a[1];
        switch (propertyCode) {
            case model.REASON_STRING_PROPERTY_CODE:
                _b = __read(decodeLengthPrefixedString(payload, index), 2), puback.reasonString = _b[0], index = _b[1];
                break;
            case model.USER_PROPERTY_PROPERTY_CODE:
                if (!puback.userProperties) {
                    puback.userProperties = new Array();
                }
                index = decodeUserProperty(payload, index, puback.userProperties);
                break;
            default:
                throw new error_1.CrtError("Unknown Puback property code: " + propertyCode);
        }
    }
    if (index != offset + propertyLength) {
        throw new error_1.CrtError("Puback packet mismatch between encoded properties and expected length");
    }
    return index;
}
function decodePubackPacket5(firstByte, payload) {
    var _a, _b, _c;
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_PUBACK) {
        throw new error_1.CrtError("Puback packet with invalid first byte: " + firstByte);
    }
    var index = 0;
    var puback = {
        type: mqtt5_packet.PacketType.Puback,
        packetId: 0,
        reasonCode: mqtt5_packet.PubackReasonCode.Success,
    };
    _a = __read(decodeU16(payload, index), 2), puback.packetId = _a[0], index = _a[1];
    if (payload.byteLength > 2) {
        _b = __read(decodeU8(payload, index), 2), puback.reasonCode = _b[0], index = _b[1];
        if (payload.byteLength > 3) {
            var propertiesLength = 0;
            _c = __read(decodeVli(payload, index), 2), propertiesLength = _c[0], index = _c[1];
            index = decodePubackProperties(puback, payload, index, propertiesLength);
        }
    }
    if (index != payload.byteLength) {
        throw new error_1.CrtError("Puback packet mismatch between payload and expected length");
    }
    return puback;
}
function decodeSubackProperties(suback, payload, offset, propertyLength) {
    var _a, _b;
    var index = offset;
    var propertyCode = 0;
    while (index < offset + propertyLength) {
        _a = __read(decodeU8(payload, index), 2), propertyCode = _a[0], index = _a[1];
        switch (propertyCode) {
            case model.REASON_STRING_PROPERTY_CODE:
                _b = __read(decodeLengthPrefixedString(payload, index), 2), suback.reasonString = _b[0], index = _b[1];
                break;
            case model.USER_PROPERTY_PROPERTY_CODE:
                if (!suback.userProperties) {
                    suback.userProperties = new Array();
                }
                index = decodeUserProperty(payload, index, suback.userProperties);
                break;
            default:
                throw new error_1.CrtError("Unknown Suback property code: " + propertyCode);
        }
    }
    if (index != offset + propertyLength) {
        throw new error_1.CrtError("Suback packet mismatch between encoded properties and expected length");
    }
    return index;
}
function decodeSubackPacket5(firstByte, payload) {
    var _a, _b, _c;
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_SUBACK) {
        throw new error_1.CrtError("Suback packet with invalid first byte: " + firstByte);
    }
    var index = 0;
    var suback = {
        type: mqtt5_packet.PacketType.Suback,
        packetId: 0,
        reasonCodes: new Array()
    };
    _a = __read(decodeU16(payload, index), 2), suback.packetId = _a[0], index = _a[1];
    var propertiesLength = 0;
    _b = __read(decodeVli(payload, index), 2), propertiesLength = _b[0], index = _b[1];
    index = decodeSubackProperties(suback, payload, index, propertiesLength);
    var reasonCodeCount = payload.byteLength - index;
    for (var i = 0; i < reasonCodeCount; i++) {
        var reasonCode = 0;
        _c = __read(decodeU8(payload, index), 2), reasonCode = _c[0], index = _c[1];
        suback.reasonCodes.push(reasonCode);
    }
    return suback;
}
function decodeUnsubackProperties(unsuback, payload, offset, propertyLength) {
    var _a, _b;
    var index = offset;
    var propertyCode = 0;
    while (index < offset + propertyLength) {
        _a = __read(decodeU8(payload, index), 2), propertyCode = _a[0], index = _a[1];
        switch (propertyCode) {
            case model.REASON_STRING_PROPERTY_CODE:
                _b = __read(decodeLengthPrefixedString(payload, index), 2), unsuback.reasonString = _b[0], index = _b[1];
                break;
            case model.USER_PROPERTY_PROPERTY_CODE:
                if (!unsuback.userProperties) {
                    unsuback.userProperties = new Array();
                }
                index = decodeUserProperty(payload, index, unsuback.userProperties);
                break;
            default:
                throw new error_1.CrtError("Unknown Unsuback property code: " + propertyCode);
        }
    }
    if (index != offset + propertyLength) {
        throw new error_1.CrtError("Unsuback packet mismatch between encoded properties and expected length");
    }
    return index;
}
function decodeUnsubackPacket5(firstByte, payload) {
    var _a, _b, _c;
    if (firstByte != model.PACKET_TYPE_FIRST_BYTE_UNSUBACK) {
        throw new error_1.CrtError("Unsuback packet with invalid first byte: " + firstByte);
    }
    var index = 0;
    var unsuback = {
        type: mqtt5_packet.PacketType.Unsuback,
        packetId: 0,
        reasonCodes: new Array()
    };
    _a = __read(decodeU16(payload, index), 2), unsuback.packetId = _a[0], index = _a[1];
    var propertiesLength = 0;
    _b = __read(decodeVli(payload, index), 2), propertiesLength = _b[0], index = _b[1];
    index = decodeUnsubackProperties(unsuback, payload, index, propertiesLength);
    var reasonCodeCount = payload.byteLength - index;
    for (var i = 0; i < reasonCodeCount; i++) {
        var reasonCode = 0;
        _c = __read(decodeU8(payload, index), 2), reasonCode = _c[0], index = _c[1];
        unsuback.reasonCodes.push(reasonCode);
    }
    return unsuback;
}
function decodeDisconnectProperties(disconnect, payload, offset, propertyLength) {
    var _a, _b, _c, _d;
    var index = offset;
    var propertyCode = 0;
    while (index < offset + propertyLength) {
        _a = __read(decodeU8(payload, index), 2), propertyCode = _a[0], index = _a[1];
        switch (propertyCode) {
            case model.SESSION_EXPIRY_INTERVAL_PROPERTY_CODE:
                _b = __read(decodeU32(payload, index), 2), disconnect.sessionExpiryIntervalSeconds = _b[0], index = _b[1];
                break;
            case model.REASON_STRING_PROPERTY_CODE:
                _c = __read(decodeLengthPrefixedString(payload, index), 2), disconnect.reasonString = _c[0], index = _c[1];
                break;
            case model.USER_PROPERTY_PROPERTY_CODE:
                if (!disconnect.userProperties) {
                    disconnect.userProperties = new Array();
                }
                index = decodeUserProperty(payload, index, disconnect.userProperties);
                break;
            case model.SERVER_REFERENCE_PROPERTY_CODE:
                _d = __read(decodeLengthPrefixedString(payload, index), 2), disconnect.serverReference = _d[0], index = _d[1];
                break;
            default:
                throw new error_1.CrtError("Unknown Disconnect property code: " + propertyCode);
        }
    }
    if (index != offset + propertyLength) {
        throw new error_1.CrtError("Disconnect packet mismatch between encoded properties and expected length");
    }
    return index;
}
function decodeDisconnectPacket5(firstByte, payload) {
    var _a, _b;
    if (firstByte != (model.PACKET_TYPE_DISCONNECT_FULL_ENCODING_311 >>> 8)) {
        throw new error_1.CrtError("Disconnect packet with invalid first byte: " + firstByte);
    }
    var index = 0;
    var disconnect = {
        type: mqtt5_packet.PacketType.Disconnect,
        reasonCode: mqtt5_packet.DisconnectReasonCode.NormalDisconnection
    };
    if (payload.byteLength > 0) {
        _a = __read(decodeU8(payload, index), 2), disconnect.reasonCode = _a[0], index = _a[1];
        if (payload.byteLength > 1) {
            var propertiesLength = 0;
            _b = __read(decodeVli(payload, index), 2), propertiesLength = _b[0], index = _b[1];
            index = decodeDisconnectProperties(disconnect, payload, index, propertiesLength);
        }
    }
    if (index != payload.byteLength) {
        throw new error_1.CrtError("Disconnect packet mismatch between payload and expected length");
    }
    return disconnect;
}
// decoders for server-decoded packets are found in the spec file
function buildClientDecodingFunctionSet(mode) {
    switch (mode) {
        case mqtt_shared.ProtocolMode.Mqtt311:
            return new Map([
                [mqtt5_packet.PacketType.Connack, decodeConnackPacket311],
                [mqtt5_packet.PacketType.Publish, decodePublishPacket311],
                [mqtt5_packet.PacketType.Puback, decodePubackPacket311],
                [mqtt5_packet.PacketType.Suback, decodeSubackPacket311],
                [mqtt5_packet.PacketType.Unsuback, decodeUnsubackPacket311],
                [mqtt5_packet.PacketType.Pingresp, decodePingrespPacket],
            ]);
        case mqtt_shared.ProtocolMode.Mqtt5:
            return new Map([
                [mqtt5_packet.PacketType.Connack, decodeConnackPacket5],
                [mqtt5_packet.PacketType.Publish, decodePublishPacket5],
                [mqtt5_packet.PacketType.Puback, decodePubackPacket5],
                [mqtt5_packet.PacketType.Suback, decodeSubackPacket5],
                [mqtt5_packet.PacketType.Unsuback, decodeUnsubackPacket5],
                [mqtt5_packet.PacketType.Disconnect, decodeDisconnectPacket5],
                [mqtt5_packet.PacketType.Pingresp, decodePingrespPacket],
            ]);
    }
    throw new error_1.CrtError("Unsupported protocol");
}
exports.buildClientDecodingFunctionSet = buildClientDecodingFunctionSet;
var DecoderStateType;
(function (DecoderStateType) {
    /**
     * We're waiting for the a byte to tell us what the next packet is
     */
    DecoderStateType[DecoderStateType["PendingFirstByte"] = 0] = "PendingFirstByte";
    /**
     * We're waiting for the VLI-encoded remaining length of the full packet
     */
    DecoderStateType[DecoderStateType["PendingRemainingLength"] = 1] = "PendingRemainingLength";
    /**
     * We're waiting for the complete packet payload (determined by the remaining length field)
     */
    DecoderStateType[DecoderStateType["PendingPayload"] = 2] = "PendingPayload";
})(DecoderStateType || (DecoderStateType = {}));
/**
 * Starting buffer size for the buffer used to hold the payload (or the remaining length VLI encoding).  Grows
 * as necessary.
 */
var DEFAULT_SCRATCH_BUFFER_SIZE = 16 * 1024;
/**
 * Decoder implementation.  All failures surface as exceptions and are considered protocol-fatal (the connection
 * must be dropped).
 */
var Decoder = /** @class */ (function () {
    function Decoder(decoders) {
        this.decoders = decoders;
        this.scratchBuffer = new ArrayBuffer(DEFAULT_SCRATCH_BUFFER_SIZE);
        this.scratchView = new DataView(this.scratchBuffer);
        this.scratchIndex = 0;
        this.remainingLength = 0;
        this.firstByte = 0;
        this.state = DecoderStateType.PendingFirstByte;
    }
    Decoder.prototype.reset = function () {
        this.state = DecoderStateType.PendingFirstByte;
        this.remainingLength = 0;
        this.firstByte = 0;
        this.scratchIndex = 0;
    };
    Decoder.prototype.decode = function (data) {
        var current_data = data;
        var packets = new Array();
        var current_state = this.state;
        var next_state = this.state;
        // we're only done when there's no bytes AND the current state's service function does not advance us to
        // the next state.
        while (current_data.byteLength > 0 || current_state != next_state) {
            current_state = this.state;
            switch (this.state) {
                case DecoderStateType.PendingFirstByte:
                    current_data = this._handleFirstByte(current_data);
                    break;
                case DecoderStateType.PendingRemainingLength:
                    current_data = this._handleRemainingLength(current_data);
                    break;
                case DecoderStateType.PendingPayload:
                    current_data = this._handlePendingPayload(current_data, packets);
                    break;
            }
            next_state = this.state;
        }
        return packets;
    };
    Decoder.prototype._handleFirstByte = function (data) {
        if (data.byteLength == 0) {
            return data;
        }
        this.firstByte = data.getUint8(0);
        this.state = DecoderStateType.PendingRemainingLength;
        this.scratchIndex = 0;
        this.scratchView = new DataView(this.scratchBuffer);
        return new DataView(data.buffer, data.byteOffset + 1, data.byteLength - 1);
    };
    Decoder.prototype._handleRemainingLength = function (data) {
        if (data.byteLength == 0) {
            return data;
        }
        var nextByte = data.getUint8(0);
        this.scratchView.setUint8(this.scratchIndex++, nextByte);
        var result = vli.decodeVli(new DataView(this.scratchBuffer, 0, this.scratchIndex), 0);
        if (result.type == vli.VliDecodeResultType.Success) {
            // @ts-ignore
            this.remainingLength = result.value;
            this.scratchIndex = 0;
            // make sure there's enough room for the payload
            if (this.remainingLength > this.scratchBuffer.byteLength) {
                this.scratchBuffer = new ArrayBuffer(this.remainingLength * 3 / 2);
            }
            this.scratchView = new DataView(this.scratchBuffer, 0, this.remainingLength);
            this.state = DecoderStateType.PendingPayload;
        }
        return new DataView(data.buffer, data.byteOffset + 1, data.byteLength - 1);
    };
    Decoder.prototype._handlePendingPayload = function (data, packets) {
        var bytesToCopy = Math.min(data.byteLength, this.remainingLength - this.scratchIndex);
        if (bytesToCopy > 0) {
            var sourceView = new Uint8Array(data.buffer, data.byteOffset, bytesToCopy);
            var destView = new Uint8Array(this.scratchBuffer, this.scratchIndex, bytesToCopy);
            destView.set(sourceView);
            this.scratchIndex += bytesToCopy;
        }
        if (this.scratchIndex == this.remainingLength) {
            this.state = DecoderStateType.PendingFirstByte;
            this.scratchView = new DataView(this.scratchBuffer, 0, this.remainingLength);
            packets.push(this._decodePacket());
        }
        return new DataView(data.buffer, data.byteOffset + bytesToCopy, data.byteLength - bytesToCopy);
    };
    Decoder.prototype._decodePacket = function () {
        var packetType = this.firstByte >>> 4;
        var decoder = this.decoders.get(packetType);
        if (!decoder) {
            throw new error_1.CrtError("No decoder for packet type");
        }
        return decoder(this.firstByte, this.scratchView);
    };
    return Decoder;
}());
exports.Decoder = Decoder;
//# sourceMappingURL=decoder.js.map