"use strict";
/*
 * Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResubscribeManager = void 0;
var mqtt5_packet = __importStar(require("../../common/mqtt5_packet"));
var AWS_IOT_CORE_MAX_SUBSCRIPTIONS_PER_SUBSCRIBE = 8;
var ResubscribeManager = /** @class */ (function () {
    function ResubscribeManager() {
        this.subscriptions = new Map();
    }
    ResubscribeManager.prototype.reset = function () {
        this.subscriptions.clear();
    };
    ResubscribeManager.prototype.onSubscribeRequest = function (topicFilter, qos) {
        this.subscriptions.set(topicFilter, qos);
    };
    ResubscribeManager.prototype.onUnsubscribeRequest = function (topicFilter) {
        this.subscriptions.delete(topicFilter);
    };
    ResubscribeManager.prototype.buildResubscribePacketList = function () {
        var e_1, _a;
        var subscribes = [];
        var currentSubscribe = undefined;
        try {
            for (var _b = __values(this.subscriptions.entries()), _c = _b.next(); !_c.done; _c = _b.next()) {
                var _d = __read(_c.value, 2), topicFilter = _d[0], qos = _d[1];
                if (currentSubscribe == undefined) {
                    currentSubscribe = {
                        type: mqtt5_packet.PacketType.Subscribe,
                        subscriptions: []
                    };
                    subscribes.push(currentSubscribe);
                }
                currentSubscribe.subscriptions.push({
                    topicFilter: topicFilter,
                    qos: qos
                });
                if (currentSubscribe.subscriptions.length >= AWS_IOT_CORE_MAX_SUBSCRIPTIONS_PER_SUBSCRIBE) {
                    currentSubscribe = undefined;
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return subscribes;
    };
    return ResubscribeManager;
}());
exports.ResubscribeManager = ResubscribeManager;
//# sourceMappingURL=resubscribe.js.map