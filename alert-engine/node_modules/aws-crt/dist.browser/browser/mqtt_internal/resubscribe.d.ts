import * as mqtt5_packet from "../../common/mqtt5_packet";
export declare class ResubscribeManager {
    private subscriptions;
    constructor();
    reset(): void;
    onSubscribeRequest(topicFilter: string, qos: mqtt5_packet.QoS): void;
    onUnsubscribeRequest(topicFilter: string): void;
    buildResubscribePacketList(): Array<mqtt5_packet.SubscribePacket>;
}
