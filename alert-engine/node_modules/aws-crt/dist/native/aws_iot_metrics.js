"use strict";
/*
 * Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.create_metrics_mqtt3 = exports.create_metrics_mqtt5 = exports.get_encoded_feature_list_mqtt3 = exports.get_encoded_feature_list_mqtt5 = exports.minimum_tls_version_metrics_value = exports.tls_cipher_preference_metrics_value = exports.certificate_source_metrics_value = exports.http_proxy_type_metrics_value = exports.socket_implementation_metrics_value = exports.offline_queue_behavior_metrics_value = exports._buildSdkMetrics = exports._setSdkMetricsFactory = exports.create_metrics = exports.merge_feature_lists = exports.protocol_version_metrics_value = exports.inbound_topic_alias_metrics_value = exports.outbound_topic_alias_metrics_value = exports.session_behavior_metrics_value = exports.retry_jitter_metrics_value = exports.MetricsFeatureId = exports.IOT_SDK_METRICS_FEATURE_VERSION = void 0;
/**
 * Native (Node) metrics encoding logic for IoT SDK metrics.
 *
 * @internal
 * @packageDocumentation
 */
const mqtt_shared = __importStar(require("../common/mqtt_shared"));
const common_metrics = __importStar(require("../common/aws_iot_metrics"));
const common_mqtt5 = __importStar(require("../common/mqtt5"));
const aws_iot_metrics_1 = require("../common/aws_iot_metrics");
const io_1 = require("./io");
const io_2 = require("../common/io");
// Re-export shared metrics API so existing callsites and specs importing
// from "./aws_iot_metrics" continue to work unchanged.
var aws_iot_metrics_2 = require("../common/aws_iot_metrics");
Object.defineProperty(exports, "IOT_SDK_METRICS_FEATURE_VERSION", { enumerable: true, get: function () { return aws_iot_metrics_2.IOT_SDK_METRICS_FEATURE_VERSION; } });
Object.defineProperty(exports, "MetricsFeatureId", { enumerable: true, get: function () { return aws_iot_metrics_2.MetricsFeatureId; } });
Object.defineProperty(exports, "retry_jitter_metrics_value", { enumerable: true, get: function () { return aws_iot_metrics_2.retry_jitter_metrics_value; } });
Object.defineProperty(exports, "session_behavior_metrics_value", { enumerable: true, get: function () { return aws_iot_metrics_2.session_behavior_metrics_value; } });
Object.defineProperty(exports, "outbound_topic_alias_metrics_value", { enumerable: true, get: function () { return aws_iot_metrics_2.outbound_topic_alias_metrics_value; } });
Object.defineProperty(exports, "inbound_topic_alias_metrics_value", { enumerable: true, get: function () { return aws_iot_metrics_2.inbound_topic_alias_metrics_value; } });
Object.defineProperty(exports, "protocol_version_metrics_value", { enumerable: true, get: function () { return aws_iot_metrics_2.protocol_version_metrics_value; } });
Object.defineProperty(exports, "merge_feature_lists", { enumerable: true, get: function () { return aws_iot_metrics_2.merge_feature_lists; } });
Object.defineProperty(exports, "create_metrics", { enumerable: true, get: function () { return aws_iot_metrics_2.create_metrics; } });
Object.defineProperty(exports, "_setSdkMetricsFactory", { enumerable: true, get: function () { return aws_iot_metrics_2._setSdkMetricsFactory; } });
Object.defineProperty(exports, "_buildSdkMetrics", { enumerable: true, get: function () { return aws_iot_metrics_2._buildSdkMetrics; } });
// ---- Native-only feature value mappers ----
/**
 * Map ClientOperationQueueBehavior to its single-character metrics value.
 *
 * Mapping: FailNonQos1PublishOnDisconnect->A, FailQos0PublishOnDisconnect->B,
 * FailAllOnDisconnect->C.
 * Returns undefined for the default behavior or any unrecognized value.
 * @internal
 */
function offline_queue_behavior_metrics_value(behavior) {
    switch (behavior) {
        case common_mqtt5.ClientOperationQueueBehavior.FailNonQos1PublishOnDisconnect: return "A";
        case common_mqtt5.ClientOperationQueueBehavior.FailQos0PublishOnDisconnect: return "B";
        case common_mqtt5.ClientOperationQueueBehavior.FailAllOnDisconnect: return "C";
        default: return undefined;
    }
}
exports.offline_queue_behavior_metrics_value = offline_queue_behavior_metrics_value;
/**
 * Detect the socket implementation and return its single-character metrics value.
 *
 * Mapping: Windows (IOCP)->B, all other platforms (POSIX)->A.
 * @internal
 */
function socket_implementation_metrics_value() {
    return process.platform === "win32" ? "B" : "A";
}
exports.socket_implementation_metrics_value = socket_implementation_metrics_value;
/**
 * Map proxy options to the single-character metrics value for proxy type.
 *
 * Mapping: HTTPS (has tls_opts)->B, HTTP->A.
 * @internal
 */
function http_proxy_type_metrics_value(proxyOptions) {
    if (proxyOptions === null || proxyOptions === void 0 ? void 0 : proxyOptions.tls_opts) {
        return "B"; // HTTPS
    }
    return "A"; // HTTP
}
exports.http_proxy_type_metrics_value = http_proxy_type_metrics_value;
/**
 * Map CertificateSource to its single-character metrics value.
 *
 * Mapping: CERTIFICATE_FILES->A, PKCS11->B, WINDOWS_CERT_STORE->C, PKCS12_FILE->E.
 * Letter "D" is reserved for Java KeyStore.
 * Returns undefined for unset or unrecognized values.
 * @internal
 */
function certificate_source_metrics_value(source) {
    switch (source) {
        case io_1.CertificateSource.CERTIFICATE_FILES: return "A";
        case io_1.CertificateSource.PKCS11: return "B";
        case io_1.CertificateSource.WINDOWS_CERT_STORE: return "C";
        case io_1.CertificateSource.PKCS12_FILE: return "E";
        default: return undefined;
    }
}
exports.certificate_source_metrics_value = certificate_source_metrics_value;
/**
 * Map TlsCipherPreference to its single-character metrics value.
 *
 * Mapping: PQ_TLSv1_0_2021_05->F, PQ_Default->H, TLSv1_2_2025_07->I.
 * Letters A-E, G, J, K are reserved for cipher preferences exposed by
 * other language SDKs but not by Node.js.
 * Returns undefined for the default or any unrecognized value.
 * @internal
 */
function tls_cipher_preference_metrics_value(pref) {
    switch (pref) {
        case io_1.TlsCipherPreference.PQ_TLSv1_0_2021_05: return "F";
        case io_1.TlsCipherPreference.PQ_Default: return "H";
        case io_1.TlsCipherPreference.TLSv1_2_2025_07: return "I";
        default: return undefined;
    }
}
exports.tls_cipher_preference_metrics_value = tls_cipher_preference_metrics_value;
/**
 * Map TlsVersion to its single-character metrics value.
 *
 * Mapping: SSLv3->A, TLSv1->B, TLSv1_1->C, TLSv1_2->D, TLSv1_3->E.
 * Returns undefined for TlsVersion.Default so the feature is omitted
 * when no minimum was configured.
 * @internal
 */
function minimum_tls_version_metrics_value(version) {
    switch (version) {
        case io_2.TlsVersion.SSLv3: return "A";
        case io_2.TlsVersion.TLSv1: return "B";
        case io_2.TlsVersion.TLSv1_1: return "C";
        case io_2.TlsVersion.TLSv1_2: return "D";
        case io_2.TlsVersion.TLSv1_3: return "E";
        default: return undefined;
    }
}
exports.minimum_tls_version_metrics_value = minimum_tls_version_metrics_value;
// ---- Encoding functions ----
/**
 * Generates the encoded feature list string for metrics from a native
 * MQTT5 client config.
 *
 * Format: "ID/Value,ID/Value,..."
 * Example: "A/B,C/A,F/5,G/A" means retryJitterMode=Full,
 * offlineQueueBehavior=FailNonQos1PublishOnDisconnect, protocol=MQTT5, socket=POSIX.
 *
 * MQTT5 connections always include:
 *  - F (protocolVersion): set to Mqtt5
 *  - G (socketImplementation): detected from platform (POSIX or IOCP)
 *
 * Conditionally includes (only when the option is explicitly set and not default):
 *  - A (retryJitterMode)
 *  - B (sessionBehavior)
 *  - C (offlineQueueBehavior)
 *  - D (outboundTopicAliasBehavior)
 *  - E (inboundTopicAliasBehavior)
 *  - H (httpProxyType)
 *  - I (certificate_source)
 *  - J (tls_cipher_preference)
 *  - K (min_tls_version)
 *
 * @param config - MQTT5 client configuration.
 * @returns The encoded feature list string.
 * @internal
 */
function get_encoded_feature_list_mqtt5(config) {
    var _a, _b, _c;
    const features = [];
    const jitter = common_metrics.retry_jitter_metrics_value(config.retryJitterMode);
    if (jitter)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.RETRY_JITTER_MODE}/${jitter}`);
    const session = common_metrics.session_behavior_metrics_value(config.sessionBehavior);
    if (session)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.SESSION_BEHAVIOR}/${session}`);
    const queue = offline_queue_behavior_metrics_value(config.offlineQueueBehavior);
    if (queue)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.OFFLINE_QUEUE_BEHAVIOR}/${queue}`);
    const topicAliasing = config.topicAliasingOptions;
    if (topicAliasing) {
        const outbound = common_metrics.outbound_topic_alias_metrics_value(topicAliasing.outboundBehavior);
        if (outbound)
            features.push(`${aws_iot_metrics_1.MetricsFeatureId.OUTBOUND_TOPIC_ALIAS_BEHAVIOR}/${outbound}`);
        const inbound = common_metrics.inbound_topic_alias_metrics_value(topicAliasing.inboundBehavior);
        if (inbound)
            features.push(`${aws_iot_metrics_1.MetricsFeatureId.INBOUND_TOPIC_ALIAS_BEHAVIOR}/${inbound}`);
    }
    // Always included
    features.push(`${aws_iot_metrics_1.MetricsFeatureId.PROTOCOL_VERSION}/${common_metrics.protocol_version_metrics_value(mqtt_shared.ProtocolMode.Mqtt5)}`);
    features.push(`${aws_iot_metrics_1.MetricsFeatureId.SOCKET_IMPLEMENTATION}/${socket_implementation_metrics_value()}`);
    if (config.httpProxyOptions) {
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.HTTP_PROXY_TYPE}/${http_proxy_type_metrics_value(config.httpProxyOptions)}`);
    }
    const certSrc = certificate_source_metrics_value((_a = config.tlsCtx) === null || _a === void 0 ? void 0 : _a.certificate_source);
    if (certSrc)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.CERTIFICATE_SOURCE}/${certSrc}`);
    const cipher = tls_cipher_preference_metrics_value((_b = config.tlsCtx) === null || _b === void 0 ? void 0 : _b.tls_cipher_preference);
    if (cipher)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.TLS_CIPHER_PREFERENCE}/${cipher}`);
    const tlsVer = minimum_tls_version_metrics_value((_c = config.tlsCtx) === null || _c === void 0 ? void 0 : _c.min_tls_version);
    if (tlsVer)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.MINIMUM_TLS_VERSION}/${tlsVer}`);
    return features.join(",");
}
exports.get_encoded_feature_list_mqtt5 = get_encoded_feature_list_mqtt5;
/**
 * Generates the encoded feature list string for metrics from a native
 * MQTT3 connection config.
 *
 * Format: "ID/Value,ID/Value..."
 *
 * MQTT3 connections always include:
 *  - F (protocolVersion): set to Mqtt311
 *  - G (socketImplementation): detected from platform (POSIX or IOCP)
 *
 * Conditionally includes:
 *  - H (httpProxyType)
 *  - I (certificate_source)
 *  - J (tls_cipher_preference)
 *  - K (min_tls_version)
 *
 * @param config - MQTT connection config.
 * @returns The encoded feature list string.
 * @internal
 */
function get_encoded_feature_list_mqtt3(config) {
    var _a, _b, _c;
    const features = [];
    features.push(`${aws_iot_metrics_1.MetricsFeatureId.PROTOCOL_VERSION}/${common_metrics.protocol_version_metrics_value(mqtt_shared.ProtocolMode.Mqtt311)}`);
    features.push(`${aws_iot_metrics_1.MetricsFeatureId.SOCKET_IMPLEMENTATION}/${socket_implementation_metrics_value()}`);
    if (config.proxy_options) {
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.HTTP_PROXY_TYPE}/${http_proxy_type_metrics_value(config.proxy_options)}`);
    }
    const certSrc = certificate_source_metrics_value((_a = config.tls_ctx) === null || _a === void 0 ? void 0 : _a.certificate_source);
    if (certSrc)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.CERTIFICATE_SOURCE}/${certSrc}`);
    const cipher = tls_cipher_preference_metrics_value((_b = config.tls_ctx) === null || _b === void 0 ? void 0 : _b.tls_cipher_preference);
    if (cipher)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.TLS_CIPHER_PREFERENCE}/${cipher}`);
    const tlsVer = minimum_tls_version_metrics_value((_c = config.tls_ctx) === null || _c === void 0 ? void 0 : _c.min_tls_version);
    if (tlsVer)
        features.push(`${aws_iot_metrics_1.MetricsFeatureId.MINIMUM_TLS_VERSION}/${tlsVer}`);
    return features.join(",");
}
exports.get_encoded_feature_list_mqtt3 = get_encoded_feature_list_mqtt3;
/**
 * Create the final AWSIoTMetrics object for an MQTT5 client on native.
 *
 * Generates the CRT feature list from the full set of MQTT5 client config,
 * including detected certificate source from the TLS context, then delegates
 * to the shared create_metrics for merging with user-supplied SDK metrics.
 *
 * @param config - MQTT5 client configuration containing all connection
 *   configuration and optional user (AWSIoTMetrics) metrics.
 * @returns The final metrics object with merged CRT and SDK features.
 * @internal
 */
function create_metrics_mqtt5(config) {
    const crtFeatureList = get_encoded_feature_list_mqtt5(config);
    return common_metrics.create_metrics(config.metrics, crtFeatureList);
}
exports.create_metrics_mqtt5 = create_metrics_mqtt5;
/**
 * Create the final AWSIoTMetrics object for an MQTT3 connection on native.
 *
 * Generates the CRT feature list from the MQTT3 connection parameters,
 * including detected certificate source from the TLS context, then delegates
 * to the shared create_metrics for merging with user-supplied SDK metrics.
 *
 * @param config - MQTT3 connection configuration containing proxy options,
 *   TLS context, and optional user (AWSIoTMetrics) metrics.
 * @returns The final metrics object with merged CRT and SDK features.
 * @internal
 */
function create_metrics_mqtt3(config) {
    const crtFeatureList = get_encoded_feature_list_mqtt3(config);
    return common_metrics.create_metrics(config.metrics, crtFeatureList);
}
exports.create_metrics_mqtt3 = create_metrics_mqtt3;
//# sourceMappingURL=aws_iot_metrics.js.map